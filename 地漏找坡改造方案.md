# 地漏最低点 + 地面找坡 · 改造方案（只出方案，不出代码）

> 适用文件：`tile-preview/index.html`（单文件、零依赖、纯本地）
> 本期范围：**地面上只有一个地漏**，整间地面朝它找坡，且坡度要**真的改变砖的排布与裁切**。
> 马桶口 / 蹲便口 / 墙水管口：本期不展开，只在第 9 节给出接入思路。

## 术语小抄（先认识这几个词，后面反复用）

| 词 | 大白话 |
|---|---|
| 找坡 | 地面不是水平的，是斜的，让水自己往低处流 |
| 坡度 | 有多斜。行话"一米降一公分" = 每走 1000mm 低 10mm = 1% |
| 分水线 | 过地漏弹的**一横一竖**两条墨线（都平行于墙），成个"十字"，把地面分成 4 块矩形斜坡 |
| 汇水线 | 分水线其实是个"凹槽底"，水顺着它流到地漏。现场也叫"谷线" |
| 套割 | 在一块砖中间挖个孔（地漏孔、水管孔都这么干） |
| 回字 | 地漏周围四块砖各切一个 45° 斜角，围出一个方口，样子像"回"字 |
| 长条地漏 | 一条 30~60 公分长的窄条地漏，装淋浴区靠墙或干湿交界处 |
| 骑线 | 分水线从整砖中间穿过时**不锯开**，整块铺过去，砖底下多抹点灰垫实（见 2.5） |

---

## 1. 核心冲突分析：为什么现有 `layoutAxis` 直接套用会出问题

### 1.1 现有引擎长什么样

`layoutAxis(L, t, g, mode, phaseOverride)`（`index.html:312`）干的事只有一件：
把**一条直线**按"砖宽 + 缝宽"这个固定步长 `p = t + g` 均分，返回一串
`{start, width, cut}` 的段。

地面用它算两次（X 一次、Y 一次），两个方向**互不干涉**，于是得到一张
"横平竖直的方格网"（`computeScene` 的 ① 段，`index.html:569-624`）。
渲染时 `paintTiles`（`index.html:1343`）就是一个两层循环 + `fillRect`：
每个格子画一个矩形。

一句话：**现在的世界只有"水平/竖直的直线"和"轴对齐的矩形"。**

### 1.2 向一个点找坡，需要什么

地漏在地面中间某个点 F，四个墙角最高，F 最低。
**过 F 弹一横一竖两条线（都平行于墙）**，成个"十字"，地面被分成 **4 块矩形**，
每块矩形自己是一个斜面，往 F 那个角倾斜。
（想象把一张方纸，沿"过中心的一横一竖"折出**两道凹槽**，四个角翘起来——
地漏就在两道凹槽的交点上，是全屋最低处。）

```
俯视（地面矩形，F = 地漏）

 ┌───────┬──────────────┐
 │       │              │
 │  ①    │      ②       │
 │       │              │
 ├───────●──────────────┤  ← 横线：过地漏，平行于前/后墙
 │       │              │
 │  ③    │      ④       │
 │       │              │
 └───────┴──────────────┘
         ↑
      竖线：过地漏，平行于左/右墙
         ● F = 全屋最低点，标高 0

 4 块矩形斜坡，每块往地漏那个角倾
 分水线只有 2 条，而且都平行于墙（= 平行于砖边）
```

**为什么必须是"十字线"，不能是"从地漏连到四个墙角"？**

对角线那种画法理论上也能排水，但**现场根本没法下刀**：

| | 对角分水线（❌ 不用） | 十字分水线（✅ 用这个） |
|---|---|---|
| 弹线 | 斜线，要拉对角线、角度靠拉尺 | 平行于墙，量个尺寸就能弹 |
| 切砖 | **斜切**，每块砖的切割角度和位置都不一样 | **直切**，切割线平行于砖边 |
| 切割机 | 每块砖都要重新对线、重新量（还得斜切） | **地面中间一把锯子都不用动**：压上缝就整列不用管，压不上也只是骑线垫灰，位置整列一致 |
| 切完的形状 | 梯形/三角，不好排 | 两块矩形，严丝合缝 |
| 能不能不切 | 不可能（斜线压不到直缝上） | **可以**：把网格挪一下，让十字线正好落在砖缝上 |

于是出现三个躲不开的冲突：

**冲突 A：分水线会穿过砖，但砖不能锯开。**
一块砖如果被分水线拦腰穿过，它一半要往左歪、一半要往右歪——
但砖是硬的平板，**折不了**。

**现场的真实解法是：不锯，让整砖"骑"在线上**（见 2.5）。
砖整块铺过去，骑在分水线上，砖底下用砂浆垫实。
这样砖底会有一点悬空，但悬空量小得可怜：

```
最大悬空量 = 坡度 × 砖宽 ÷ 4
   300 砖 @1%  →  0.75mm
   600 砖 @1%  →  1.50mm      ← 砂浆层有 10~30mm 厚，这点量直接吸掉
   800 砖 @2%  →  4.00mm      ← 只有这种大砖 + 陡坡才真要处理
```

**这是本方案最重要的一次修正**：初稿写的是"沿线锯开"，
那是书本上的理想做法，**现场从来没人这么干**——地面中间的整砖一律整块铺过去。

**冲突 B：骑线的悬空量太大时怎么办。**
大砖 + 陡坡（800 砖 @2% → 4mm）时，砖底下垫 4mm 灰会让砖比周围高出一截。
这时的折中办法是**调起铺相位，把砖缝挪到分水线上**（2.4.4）——
注意是**挪砖缝，不是挪分水线**，见下面这条例外说明。

**冲突 C：地面变斜以后，墙砖的"脚"站在哪。**
现有"墙压地"规则（`index.html:639-655`）让墙最底一排铺整砖、裁砖甩到顶。
地面一斜，**每面墙的墙根就变成一条 V 形折线**（两端高、中间低，
最低点在"地漏到这面墙的垂足"那儿，但那个最低点**不是 0**，而是
`坡度 × 地漏到这面墙的距离`）——底排整砖"从地面起"这句话就不成立了。

### 1.3 明确取舍（不说"标个位置就行"）

| 取舍点 | 选哪边 | 为什么不选另一边 |
|---|---|---|
| 真曲面（圆锥漏斗） vs 平面拼 | **平面拼** | 真漏斗每一点都精确指向地漏，但砖是平的，**现实里根本贴不出来**。平面拼是唯一能照着贴的做法 |
| 砖跟着斜面歪 vs 砖平放、灰层找坡 | **砖跟着斜面走**（灰层厚度不一样） | 这才是真实施工；"砖平放"等于没找坡，水会积在砖面上 |
| 沿线锯开 vs 整砖骑线（本轮改的） | **整砖骑线，不锯** | 骑线悬空量 = 坡度×砖宽÷4，300 砖 @1% 只有 0.75mm，砂浆完全吸得住。沿分水线锯砖是书本理想做法，现场从没这么干 |
| 挪砖缝 vs 挪分水线（本轮改的） | **只挪砖缝**（调起铺相位） | **分水线不能挪**：h = k(\|x−线\| + \|y−地漏y\|)，线一挪最低点就跟着跑。算过：线挪 150mm 到砖缝上，最低点就跑到离地漏 150mm 处，水积在离洞口 97mm 的地方 |
| 对角分水线 vs 十字分水线（本轮改的） | **十字分水线**（过地漏作平行于墙的一横一竖） | 对角线现场没法下刀：每块砖斜切、角度位置各不相同。十字线是直切、整列同一位置、还能压到砖缝上，压上了中间一块砖都不用管 |
| 每个分区的高度怎么给 | **统一按"横竖各分担一半"**（L1 距离，见 4.3） | 若按"各分区朝自己对角方向斜"，两相邻分区在分水线上**高度对不上**，会出现几毫米的台阶——水会积在台阶上。连续性排第一 |
| 3D 里地面真起伏 vs 平面贴图 | 本期 **平面贴图**（切块 + 明暗 + 地漏口），真起伏留作增强 | 起伏对"照着贴"帮助不大，工作量却集中；先把 2D 做对 |

---

## 2. 推荐方案（通俗版）

### 2.1 两个候选做法

> 前提：**"十字分水"只对点状地漏成立**（方的、圆的、回字的，最低处是一个点）。
> 长条地漏的最低处是**一条线**，分区方式不一样——见 **2.4**。

#### 做法 A：十字分水线（推荐，作为默认）

地漏一点 F 最低，**过 F 弹一横一竖两条线（都平行于墙）**，
把地面切成 **4 块矩形斜坡**，每块自己是一个平面。

- 每块矩形内，高度是**线性**变化的：靠地漏那个角最低（0），对角最高。
- 分水线两侧**高度接得上**（线两边算出来一样高），所以**没有台阶**；
  分水线处是个折角（其实是个"凹槽底"，水顺着它流到地漏）——真实地面就是这样。
- 砖怎么贴（**整砖一律不锯**）：
  - 整块落在某一块矩形里的砖 → 整砖平铺，跟着那块斜（砖底灰层一边厚一边薄）。
  - **被分水线穿过的砖 → 骑线，整块铺过去，底下的缝用灰垫实**（2.5 有算法和数值）。
  - 地漏周围那 1~4 块砖 → **回字对角切 / 套割**方孔圆孔（**只有这几块要切**，见 2.4.3）。
  - 想连骑线都省掉 → 把整片网格挪一点，让十字线正好压在砖缝上（2.4.4）。

- 优点：墨线好弹、**地面中间一块砖都不用切**、排水好。
- 缺点：分区内部的最陡方向固定是 **45°**（不是严格指向地漏，见 4.3 与第 7 节）；
  骑线的砖底下要垫一点灰（绝大多数情况不到 1mm）。

#### 做法 B：回字套裁（不推荐当默认，只在地漏居中的"讲究活"里用）

以地漏为中心一圈一圈往外排整砖，四个角用 L 形补块。

- 优点：好看、对称、缝都是整的。
- 缺点：切砖极多、拼缝多、圈与圈之间容易形成小台阶（**积水**）、对手艺要求高。
- 结论：不默认开启；可以在"地漏正好在房间正中"时给一句提示"如追求对称，可考虑回字套裁（费工）"。

#### 附带建议（不是第三种做法）

地漏如果**紧贴某条边或某个角**，四块矩形里会有一两块变得又扁又窄，切出来的砖很碎。
这时程序应**提示**（不自动切换）："地漏离左边只有 XX mm，十字线那一列要切下的条只有 XX mm，
建议把整片砖往右挪 XX mm，或改用长条地漏做单向找坡"。

### 2.2 现场工人怎么贴（写给装修小白的步骤）

1. **定位置**：按图纸量出地漏中心"离左墙多少、离后墙多少"，在地上点个点。
2. **弹十字线**：过这个点弹**两条**墨线——一条平行于前/后墙、一条平行于左/右墙。
   现在地面被分成了 4 块"斜坡田"（像田字的四格）。
3. **打灰找坡**：水泥砂浆按"横着每米降一公分、竖着每米降一公分"打。墙角最厚、地漏最薄。
   拿 1 米靠尺 + 水平尺量：**沿着墙量一次、垂直墙量一次，两个方向都要够坡**。
4. **铺砖（地面中间一律不切砖）**：
   - 整砖整块铺过去。**骑在墨线上的那几块不用锯**——照铺，
     只是这几块砖底下要多抹一点灰（程序会告诉你最多垫几毫米，一般不到 1mm）。
   - 铺的时候用橡皮锤敲实，让灰把砖底填满、不空鼓。
5. **只有地漏周围要切**：地漏那 1~4 块砖做回字对角切/套割（2.4.3），
   孔比面板小一圈，让面板压住毛边。
6. **倒一盆水试水**——**水能自己走、不往门口跑，就对了**。

> 一句话给业主听：卫生间地面就是**一张沿中心折了两道凹槽的方纸**，
> 四个角翘起来，地漏在两道凹槽的交点（最低处）。
> 砖跨在凹槽上就会跷，跷了就得锯开——只要把砖缝挪到凹槽上，就一块都不用锯。

### 2.3 本期输出的"坡度真影响排版"到底体现在哪

不是画个箭头了事，是这四件事会变：

1. **起铺点会变** —— 为了**让十字线正好压在砖缝上**（压上了就一块砖都不用骑线），
   程序会把整片网格往左/往前挪几毫米到几十毫米
   （`layoutAxis` 本来就有 `phaseOverride` 这个口子，见第 4 节）。
2. **砖底下要多垫灰** —— 骑线那几块砖不再是"平的"，程序给出
   **每块最多垫多少毫米**（2.5 的公式：坡度 × 砖宽 ÷ 4）。普通 300 砖 @1% 只有 0.75mm。
3. **地漏周围那几块要切** —— 回字对角切 / 套割，程序给出切口位置与套割尺寸。
   **地面中间其他砖一块都不切**（这是本轮改的核心）。
4. **墙砖底口会变** —— 墙根地面是 V 形折线，墙最底一排砖要么跟着斜切，
   要么按最高点起（见第 6 节）。

---

### 2.4 地漏造型：四种，各有各的找坡和裁砖（新增）

> 这一小节是补的。前面把地漏当成一个"点"，现实里它至少长四种样子，
> **造型直接决定地面怎么斜、砖怎么切**。
>
> **别搞混两个"回字"**：
> 2.1 的"做法 B · 回字套裁"说的是**整片地面的排砖图案**（一圈一圈铺）；
> 这一节的"回字地漏"说的是**地漏本身的造型**（外方框套内方框，中间能嵌砖）。
> 两件事，只是名字一样。
>
> 下面的规格数字取自市面常见产品（潜水艇、科勒、德房家及国产常规款），
> 程序里做成**可改的默认值 + 提示**，不写死，以手上那款的说明书为准。

### 2.4.1 四种造型速查

| 造型 | 常见规格（mm） | 装在哪 | 找坡 | 切砖量 | 适合谁 |
|---|---|---|---|---|---|
| **方形** square | 100×100（最多）、120×120、150×150；长款 100×200、150×300 | 任意位置 | 十字分水 4 块 | 少（**常能做到“中间一块都不用管”**） | 绝大多数卫生间 |
| **圆形** round | φ90、φ100，少数 φ120 | 任意位置 | 十字分水 4 块 | 少（**必须整砖开孔**） | 老房改造、管道已定 |
| **回字 / 隐形** concealed | 100×100、150×150，也有 100×200、150×300；面板厚 30~50 | 任意位置，讲究颜值 | 十字分水 4 块 | 多（四块砖对角切） | 想"看不见地漏" |
| **长条 / 线性** linear | 面板宽 25 / 30 / 32 / 64（成套常见 300×80、500×100、600×150）；长 300~1524，**可现场裁剪** | 靠墙，或干湿交界（取代挡水条） | **单坡 / 双坡，≤1%** | **最少**（但长条压住那几块必切） | 淋浴房、老人房、无障碍 |

**长条地漏的排水能力**（选长度时用，国标下限是 0.3 L/s = 18 L/min）：

| 规格 | 排水量 | 够谁用 |
|---|---|---|
| 300×80 | 30~40 L/min | 手持花洒（8~12 L/min）绰绰有余 |
| 500×100 | 40~50 L/min | 大顶喷（15~25 L/min） |
| 600×150 | 50~60 L/min | 顶喷 + 手持同时开 |

### 2.4.2 造型决定"地面往哪斜"（核心区别）

**点状地漏的最低处是一个点，长条地漏的最低处是一条线。**

```
① 点状（方/圆/回字）            ② 长条 · 靠墙            ③ 长条 · 房间中

 ┌───────┬──────────┐          ┌──────────────┐        ┌──────────────┐
 │   ↘   │    ↙     │          │      ↓       │        │      ↓       │
 │       │          │          │     ↓↓       │        │   ↓       ↓  │
 ├───────●──────────┤ ←横线     │    ↓↓↓       │        │  ↓        ↓  │
 │   ↗   │    ↖     │          │   ↓↓↓↓↓      │        │ ▓▓▓▓▓▓▓▓▓▓  │
 │       │          │          │  ↓↓↓↓↓↓      │        │  ↑        ↑  │
 └───────┴──────────┘          │ ▓▓▓▓▓▓▓▓     │        │   ↑       ↑  │
         ↑ 竖线                 └──────────────┘        │      ↑       │
 4 块矩形，各朝地漏角斜          整片朝长条斜             └──────────────┘
 分水线 2 条（一横一竖）         分水线 0 条              两侧各朝长条斜
                                贴墙那面墙根等高          分水线 0 条
```

- **点状**：过地漏弹**一横一竖两条线**，分成 4 块矩形，每块朝地漏那个角斜。
- **线状**：长条自己就是最低线，整片朝它斜。**没有分水线**，
  只在长条两端各有一条"垂直于长条"的分区线（那两块角上的区域要朝长条的端点斜）。

顺带两个真实差异（都写进第 6 节的规则表）：

- 长条地漏**坡度只要 ≤1%**（传统点状要 1.5%~3%），因为它是一条线在收水，不是一个小点。
- 长条**平行于某面墙**时，那面墙的墙根高度**处处相等**，墙砖底排完全不受影响（只有对面那面墙要处理）。
  点状则是**四面墙**的墙根都是 V 形折线。

### 2.4.3 按实际地形尺寸选造型、定切法

**第一步：看户型，定造型**

| 你家的情形 | 推荐 |
|---|---|
| 卫生间 ≤ 4㎡，不追求隐形 | 方形 100×100 |
| 想"看不见地漏" | 回字 100×100（**砖选 300×300 以下**，小砖切角好操作） |
| 淋浴区、地面短边 ≤ 1.8m | 长条 300~600，**靠墙装**，单坡 |
| 老人 / 轮椅 / 不想做挡水条 | 长条装在**干湿交界**，双面找坡，取代挡水条 |
| 砖 ≥ 600×600 的大板 | 方形或圆形**单砖套割**（大砖切四角容易裂，别做回字） |
| 最短边 > 3m 的大卫生间 | 一个地漏不够（见第 7 节），本期不支持，需人工拆成两个区 |

**第二步：看地漏落在砖的哪个位置（这一步决定切几块）**

```
   B 落在砖心（最好）        C 落在四砖交角（次好）       A 压在砖缝中段（最糟）
   一块砖中间开个孔         四块砖各切一个 45° 角        要切两块砖，易出窄条

   ┌─────────────┐         ┌──────┬──────┐          ┌──────┬──────┐
   │             │         │      │      │          │      │      │
   │             │         │   ╲  │  ╱   │          │      ●      │
   │      ●      │         ├──────●──────┤          │      │      │
   │             │         │   ╱  │  ╲   │          │      │      │
   │             │         │      │      │          │      │      │
   └─────────────┘         └──────┴──────┘          └──────┴──────┘
                            ● 卡在十字缝交点         ● 卡在一条缝的中间
                            四块砖各切一角就行       孔跨两块砖，毛边外露
```

设 `a` = 地漏中心到左边砖缝的距离，`b` = 到上边砖缝的距离，`t` = 砖净宽（不含缝）：

| 落点 | 怎么判 | 怎么做 | 切几块 |
|---|---|---|---|
| **砖心** | `a ≈ t/2` 且 `b ≈ t/2` | **单砖套割**：一块砖中间挖孔 | 1 |
| **四砖交角** | `a ≈ 0 或 t`，且 `b ≈ 0 或 t` | **回字对角切**：四块砖各切一个 45° 角，围出方口 | 4（每块只切一个角，最省料） |
| **压在缝上** | 一个在缝上、一个在砖中间 | **最糟**：切两块砖，容易切出窄条 | 2 + 风险 |
| **离缝太近** | `a` 或 `b` < 留缝 + 20mm | 孔会"骑缝"，毛边露在缝上，丑且易崩 | 必须挪 |

程序要输出一句人话，例如：
- "地漏现在压在砖缝上，往左挪 68mm 就落在四砖交角，可少切 2 块砖"
- "地漏离砖缝只有 12mm，开孔会骑缝，建议挪 30mm"

**第三步：按造型切**

| 造型 | 切法 | 师傅手上的动作 | 留缝 / 高低 |
|---|---|---|---|
| 方形 | **十字套割**：一块砖上划出方孔 | 四角先钻孔，切割机沿线切，角磨机修角 | 每边留 **2~3mm** |
| 圆形 | **圆孔套割**：开孔器钻 | **从砖背面钻**（正面不崩边），转速 ≤3000 转/分 | 孔径比地漏**大 5mm** |
| 回字 | **四砖对角切** | 地漏周围四块砖各切一个 45° 斜角，围成方口 | 每边留 2~3mm；地漏中心低于四周砖 **1~2cm** |
| 长条 | **整排直切**（理想是整排不切） | 沿长条两条长边各切一刀，切下的窄条扔掉 | 面板装完比地面**低 1~2mm** |

精度：孔/口的误差控制在 **±1.5mm** 以内，否则地漏盖不严。
损耗：普通瓷砖多备 **5%~8%**，大理石等脆料备 **10%**。

### 2.4.4 分水线"压砖缝"：本方案真正省工的一招

**先把话说准：只有点状地漏能做到"分水线压缝 → 中间一块砖都不用管"，长条做不到。**

- **点状（方/圆/回字）**：分水线是一横一竖两条直线，只要把整片网格挪一下，
  让**十字线正好落在砖缝上**，就**一块砖都不用管**（不切也不垫灰）——每块砖都整块落在自己
  那个分区里，只是跟着坡微微倾斜。这才是本方案最大的省工点。
  压不上缝也没关系：**让砖骑线、底下垫灰**，照样不锯（见 2.5）。
- **长条**：长条要占掉一条 64mm 宽的地，被它压到的砖**一定得切**
  （除非长条宽度正好等于整数个"砖宽+缝"——现实里没有 300mm 宽的长条）。
  它的优势是切得极少、全直刀、不出窄条。

优化目标：**让分水线 / 长条边缘落在砖缝上**。这件事**不用扫描，一步就算出来**：

```
目标：让分水线（或长条左边缘）X0 落在一条砖缝上
已知：该轴的砖距 p = 砖宽 + 缝宽
解：  ideal = X0 mod p                       ← 想要的理论相位
     若这个相位让边缘裁砖 ≥ 砖宽 1/3 → 直接用，本列/本排零骑线（压上缝）
     若不满足 1/3 老规矩 → 在合法相位里取离 ideal 最近的，
                          退一步求"切下来的条 ≥ minPiece(50mm)"
```

对比：改成分水线之前，点状方案要在约 150 个候选里**扫描**，还躲不开斜切；
现在**一步闭式求解**，而且常常直接命中"压缝"。

**压上缝最好，压不上也不切砖——让砖骑线**（2.5 有完整说明）。
所以这一节的效果不是"少切几块砖"，而是：

| 结果 | 师傅要干什么 |
|---|---|
| **压上缝** | 一块砖都不用管，直接整砖铺过去 |
| **没压上** | 有几块砖**骑线**，底下多抹一点灰（一般不到 1mm），**照样不锯** |

**切砖数只统计"真正动锯子的"**（2.5.3：只有地漏周围和靠墙收边）：

| 字段 | 含义 | 能不能做到 0 |
|---|---|---|
| `plan.drainCuts` | **地漏周围**要切的砖（回字对角切 / 套割） | **永远不能**，地漏总得开个孔 |
| `plan.edgeCuts` | **靠墙收边**要切的砖（老规矩，跟找坡无关） | 视房间尺寸而定 |
| `plan.cutCount` | 两者去重合计 | ≥ `drainCuts` |

```
算例（已在 test.js 里跑通）：地面 3020×1510、砖 300、缝 2、地漏落在 (1510,755)
   理论相位 X = 1510 mod 302 = 0     Y = 755 mod 302 = 151
   压上缝 → 骑线砖数 = 0，最大悬空量 = 0
            drainCuts = 4（洞口骑在十字缝交点上，回字对角切，每块啃掉约 51×53mm）
   没压缝 → 骑线砖数 = 10，最大悬空量 = 0.5mm（垫灰即可，不锯）
```

**界面文案要说实话**，别夸大也别吓人：
- 压上缝："十字分水线已压在砖缝上，**一块砖都不用管**；地漏孔另需切 **4** 块（回字对角切）"
- 没压缝："有 **10** 块砖骑在分水线上，**不用切**，底下最多垫 **0.5mm** 灰；地漏孔另需切 **4** 块"

**附带发现：长条的长度也能挑。**
长条两端各有一条分区垂线，两条都压上缝 → 这两条线穿过的砖**整列不用管**（不用切也不用垫灰）。
规律是 **`len mod 砖距 ∈ {0, 缝宽, 砖宽}`**（已扫描验证）：

| 长度 | 300 | 302 | 604 | 906 | 1208 | 400 | 500 | **600** | 800 | **900** | 1200 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 两端都压缝 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | **❌** | ❌ | **❌** | ❌ |

**注意：市面上最常见的 600 和 900 反而不行**，能用的是 604 / 906（302 的整数倍）
和 300（= 302 − 缝宽 2）。好消息是长条**可以现场裁剪**（百度百科：长度可定制或现场裁剪），
所以程序应该直接建议："把长条裁到 **604mm**，两端正好压在砖缝上，这两条线穿过的砖不用管。"
（长条**本体**压住的那几块还是要切——那是 `drainCuts`，跟分区线是两回事，见 7.15。）

### 2.4.5 两条"够不够"的校核（按真实尺寸）

1. **抬高量校核**（所有造型）：
   `最远墙角抬高量 = 坡度 × 地漏到最远角的距离`
   例：3.5m × 1.5% = **53mm**。
   **> 30mm 就报警**：门槛石、门扇、过门石全得跟着抬，很麻烦。
   此时应降坡度，或别只用一个地漏。
2. **排水量校核**（长条专用）：按 `len` 查一张内置小表

   | 长条长度 | 能排多少 | 手持花洒 12 | 大顶喷 25 | 顶喷+手持 40 |
   |---|---|---|---|---|
   | 300 | 35 L/min | ✅ | ✅ | ❌ 差 5 |
   | 500 | 45 L/min | ✅ | ✅ | ✅ |
   | 600 | 55 L/min | ✅ | ✅ | ✅ |

   不够就提示换长一号。中间长度取最接近的一档（不插值——产品是分档卖的）。


### 2.5 整砖骑线：地面中间一块砖都不锯（本轮改的核心）

> 这一节回答一个问题：**分水线穿过整砖时怎么办？**
> 初稿的答案是"沿线锯开"，那是错的——现场从来没有沿分水线锯砖的。
> 正确答案是：**整砖铺过去，骑在线上，底下垫灰。**

#### 2.5.1 骑线会悬空多少

砖跨 `[x0, x1]`、折线在位置 `a` 时，那一块的地面是个 V 形
（两端分别高出 `k·dL` 和 `k·dR`）。砖是平的，铺上去后砖底最大悬空量是：

```
悬空量 = k × dL × dR / (dL + dR)
最坏情况（折线正过砖心，dL = dR = 砖宽/2）= k × 砖宽 / 4 = 坡度 × 砖宽 ÷ 4
```

| 砖宽 | 坡度 1% | 1.5% | 2% | 3% |
|---|---|---|---|---|
| 300 | **0.75** | 1.13 | 1.50 | 2.25 |
| 400 | 1.00 | 1.50 | 2.00 | 3.00 |
| 600 | 1.50 | 2.25 | 3.00 | 4.50 |
| 800 | 2.00 | 3.00 | **4.00** | **6.00** |

（单位 mm。**≤1.5mm 直接垫灰，不用管；≤3mm 建议压缝；>3mm 必须压缝或降坡度。**）

砂浆层有 10~30mm 厚，差个 1mm 根本看不出来——**这就是所有卫生间地砖的实际做法**。

#### 2.5.2 悬空量大时的折中办法：挪砖缝，不是挪分水线

有人会提议"把分水线挪到最近的砖缝上"。**这一侧是不行的**，算给你看：

```
地面 2000×1500，地漏在 (1000, 750)，洞口半宽 53mm，坡度 1%
高度公式 h = k ×(|x − 分水线| + |y − 地漏y|)，最低点就在"分水线 × 地漏y"那个交点上

  分水线位置      最低点        地漏处高度    结论
  ─────────────────────────────────────────────────────
  1000（穿过地漏）  (1000,750)    0.00mm     ✅ 水直接进洞口
   850（挪左砖缝）   (850,750)    1.50mm     ❌ 水积在离洞口 97mm 处
  1150（挪右砖缝）  (1150,750)    1.50mm     ❌ 水积在离洞口 97mm 处
```

**分水线一挪，最低点就跟着挪走，水就不往地漏流了。**
所以能挪的只有**砖缝**——也就是调起铺相位（2.4.4），让砖缝正好落在分水线上。
效果完全一样（压上缝 = 一块砖都不骑线），但不破坏排水。

#### 2.5.3 什么时候才真的要切砖

只有两种情况允许切砖，**且都不在地面中间**：

1. **地漏周围那 1~4 块**（回字对角切 / 套割）——本来就要开孔，见 2.4.3。
2. **房间四周靠墙的收边**（边缘裁砖 ≥ 1/3 那条老规矩照旧）。

地面中间的整砖，**一律不锯**。

---

## 3. 数据模型改动（地面卡片加什么字段）

现有地面卡片（`index.html:853`）：`{ id:"floor", type:"floor", name:"地面", w, h }`。
新增一个 `drain` 子对象（**全部可缺失，缺了就是不找坡**，老存档零影响）：

```js
// 地面卡片新增
drain: {
  on: false,          // 是否做找坡（总开关）

  // —— 造型（2.4 新增）——
  kind: "square",     // "square" 方形 | "round" 圆形 | "concealed" 回字/隐形
                      // | "linear"   长条/线性
  // —— 定位（师傅量的两个数；图坐标 x/y 由它们换算，不入库）——
  fromLeft: null,     // 地漏中心 离"左墙" mm
  fromBack: null,     // 地漏中心 离"后墙" mm
  rot: 0,             // 【长条专用】0 = 沿地面长边横放；90 = 沿地面短边竖放
                      //              点状三种忽略这个字段
  // —— 尺寸（默认值按 2.4.1 的常见规格填，师傅可改）——
  size: 100,          // 点状：边长或直径 mm（100 / 120 / 150）
  len: 300,           // 长条：面板长度 mm（300~1524，可按现场裁）
  wide: 64,           // 长条：面板宽度 mm（25 / 30 / 32 / 64 / 80 / 100）
  gap: 3,             // 套割留缝 mm：方 2~3、圆 5、回字 2~3、长条 2~3（见 2.4.3）
  // —— 长条专用安装方式 ——
  against: null,      // null = 在房间中间（双坡）
                      // "left"|"right"|"front"|"back" = 贴哪条边（单坡）
  shower: "hand",     // 【长条专用·校核用】"hand" 手持 12 / "rain" 大顶喷 25
                      //                    / "both" 顶喷+手持 40（单位 L/min）
  // —— 找坡与裁切 ——
  drop: 10,           // 坡度：横、竖两个方向各自"每 1000mm 降几 mm"
                      // 朝地漏 45° 方向的真坡度 = drop × 1.414
                      // 建议 7~14（合成 1%~2%），校验范围 5~21
  cutStyle: "auto",   // "auto" 自动挑 | "hole" 单砖套割 | "corner" 回字对角切
  fitRidges: true,    // 点状：微调起铺点，让分水线压在砖缝上（默认开；
                      //       压上 = 零骑线，压不上 = 骑线垫灰，都不锯砖）
  fitEdges: true,     // 长条：微调起铺点，让长条长边压在砖缝上（默认开，见 2.4.4）
  minPiece: 50,       // 切出来的块最短边不得小于这个 mm 数（小于就报警）
}
```

几个字段的取值约束（写成 `validateCard` 里的校验）：

- `kind` 为 `linear` 时，必须填 `len` / `wide` / `rot`；`size` 不用。
- `kind` 为点状三种时，必须填 `size`；`len` / `wide` / `rot` 忽略。
- `len` 必须在 200~2000 之间，`wide` 在 20~200 之间，`len > wide`。
- 长条贴边时，中心线到该墙的距离应 ≥ `wide/2 + 20`（给地漏槽体留位置）。
- 任何造型，地漏外轮廓（含留缝）必须完整落在地面内，且离最近的墙 ≥ `gap + 20`。

**只存 `fromLeft` / `fromBack`，还是只存 x / y？**
建议：**只存师傅量的两个数** `fromLeft` / `fromBack`，图坐标 (x,y) 由
`deriveEdges(state.doorEdge)`（`index.html:288`）换算出来，**不入库**。
理由：门换到别的边时，图坐标要跟着变，存在库里会不一致。换算规则：

先用 `deriveEdges(doorEdge)`（`index.html:288`）查出"后墙/左墙"落在**图的**哪条边，
再沿那条边的方向取补。**注意：后墙不在图的上边，门在下边时后墙是图的下边**
（`drawFloor` 的方位徽标就是这么画的，`index.html:1755`）——这里极易搞反。

| 门在 | 后墙 = 图哪边 | 房间左边 = 图哪边 | 换算（W/H 为地面长/宽） |
|---|---|---|---|
| 下边 bottom（默认） | 下边 | 左边 | `x = fromLeft`，`y = H − fromBack` |
| 上边 top | 上边 | 右边 | `x = W − fromLeft`，`y = fromBack` |
| 左边 left | 左边 | 上边 | `x = fromBack`，`y = fromLeft` |
| 右边 right | 右边 | 下边 | `x = W − fromBack`，`y = H − fromLeft` |

> 这个换算必须写成一个纯函数 `drainToDiagram(card, state)` 放进引擎块里，
> 这样 `node test.js` 能直接测"门在四条边上时地漏标的位置对不对"。

**老存档兼容**：`load()`（`index.html:859`）不动，不换 `STORE_KEY`、不做迁移。
读取处统一写 `const dr = card.drain && card.drain.on ? card.drain : null`。
老用户升级后地漏功能默认关闭，其他一切照旧。

**为什么不存"坡度百分比"而存"每米降几毫米"？**
师傅嘴上说"一米降一公分"，不说"百分之一"；而且现有 `parseMM`（`index.html:875`）
只认**正整数**，存整数能直接复用，不用为小数改输入校验。

**`drop` 到底量的是哪个方向的坡？**（改成十字分水线后这一点必须说清）

十字分水线下，每个分区的水是**斜着 45° 朝地漏流**的，但师傅拿靠尺量的时候，
只能**沿墙方向**或**垂直墙方向**量。所以：

| 量法 | 数值 | 关系 |
|---|---|---|
| 横向（平行前/后墙）每米降 | `drop` mm | 靠尺沿墙量到的就是这个 |
| 竖向（平行左/右墙）每米降 | `drop` mm | 同上 |
| **朝地漏 45° 方向**每米降 | `drop × 1.414` mm | 两个方向合起来的真正坡度 |

- 建议取值 **7~14**（对应合成坡度 1%~2%，正好是规范区间）。
- 校验范围 **5~21**：低于 5（合成 0.71%）排水太慢；高于 21（合成 3%）站不住脚。
- 长条地漏可以取到下限 **7**（合成 1%），规范要求它 ≤1%。

---

## 4. 引擎改动（改哪、加哪、怎么复用）

### 4.1 结论先说

- **`layoutAxis` 一个字都不用改。** 它已经有 `phaseOverride` 参数，
  找坡要做的事就是"给它一个算好的 phase"。
- **`computeScene` 只在 ① 段末尾加一小段**（地面算完、进墙之前）。
- **新增 14 个纯函数**（6 个是 2.4 造型改造补的，2 个是 2.5 骑线补的；
  **已落地 7 个**，待实现 7 个，见 4.2 表的 ✅ 标记），全部塞进
  `/*ENGINE-START*/ … /*ENGINE-END*/` 之间，
  这样 `test.js`（`test.js:11-16` 用正则抽引擎段）能原样测到，**零依赖约束不破**。
- **四种地漏造型共用同一套下游**：关键是把差异收敛到一个"最低处形状"上，
  见 4.2 的 `drainShape` 与 4.5 的统一模型。

### 4.2 新增的纯函数（名字 / 输入 / 输出 / 算法）

| 函数 | 干什么 | 怎么算（纯原生算术，无库、无迭代求解） |
|---|---|---|
| `drainToDiagram(drain, card, doorEdge)` ✅已实现 | 师傅量的 (离左, 离后) → 图坐标 (x, y) | 复用 `deriveEdges(doorEdge)` 查出"后墙/左墙"落在图的哪条边，再沿那条边取补（第 3 节四行表）。**不自己写映射表**，避免和引擎别处不一致 |
| `drainShape(drain, card, doorEdge)` ✅已实现 | **把四种造型归一化**成引擎只认的两种"最低处" | 返回 `{ isLine, point, seg, wide, open, drop }`。`point:{x,y}` 或 `seg:{ax,ay,bx,by}`（最低线段）；`open` 是洞口（含留缝）的轴对齐矩形；`drop` 顺带带出来给下游用。下游只看这个结果，**不认识 square / round / linear 这些词** |
| `lowDist(shape, x, y)` ✅已实现 | 到"最低处"的**横竖距离之和** | 4.3 那条公式，就这一个函数。点状：`|dx| + |dy|`；线状：横向超出量 + 竖向超出量（超出量指在线段范围外时才算） |
| `slopeZones(card, shape, drop)` ✅已实现 | 算分区（不再固定 4 块） | 见 **4.3 / 4.5**。返回**轴对齐矩形分区数组**，每项 `{ x, y, w, h, hc:[左上, 右上, 左下, 右下] }`。**注意字段名是 `hc`**——`h` 是矩形本身的高度，别混（实现时踩过这个坑）。零宽/零高的退化块直接丢掉 |
| `slopeHeightAt(zones, x, y)` ✅已实现 | 查任意一点的高度 mm | ① 找点落在哪个矩形分区（边界上取哪个都一样，模型保证连续）② 该矩形内**双线性插值**（四点共面，双线性 == 平面插值，结果精确）。比三角形重心坐标还简单 |
| `rideGap(k, t, dL)` ✅已实现 | 砖骑在分水线上时的**砖底悬空量** mm | `k × dL × dR / t`（dR = t − dL），最坏 = `k × t / 4`。见 2.5.1 |
| `rideReport(k, axis, fold)` ✅已实现 | 一条轴上哪些砖骑线、最大悬空多少 | 遍历段，找 fold 落在哪块砖里，返回 `{ count, max, worst }`。**压上缝时 count = 0** |
| `drainRidgeLines(card, shape)` | **分水线**：只用来弹墨线、算骑线悬空量，**不产生任何切割** | 点状 → **一横一竖 2 条**（过地漏、平行于墙）；长条 → **垂直于长条、过长条两端的 2 条**。**全部轴对齐**，没有斜线。<br>**注**：原名 `drainCutLines`（来自"沿线锯开"的初稿）。2.5 之后分水线一律不锯砖，名字里的 cut 会误导，已连同 `test.js` 的契约骨架一起改成 `drainRidgeLines` |
| `drainOpening(shape)` | 要"挖掉"的闭合轮廓 | 方/回字 → 正方形；圆 → 圆（本期先当外接正方形近似，见 4.7 的注）；长条 → 矩形 |
| `cutRectByLines(rect, lines)` | 一块矩形砖被若干条直线切成几个多边形 | **Sutherland–Hodgman 半平面裁剪**：拿每条线依次"切"这个矩形，每次保留线的一侧、把穿过的边求交点（一元一次方程，无迭代）。约 30 行原生 JS。返回 1~N 个凸多边形。<br>**2.5 之后不再喂分水线给它**（分水线穿过整砖一律不锯，改走 `rideReport`）；它只服务"长条本体那两条长边的直切"和靠墙收边 |
| `cutRectByHole(rect, hole)` | 一块砖被地漏口挖掉一块后剩什么 | 本期只做**矩形洞**（长条必须用；方/回字同款）。做法是沿着洞的边把矩形切成"最多 4 个 L 形/矩形块"，或直接标记 `hasHole:true` 并输出**孔中心距砖四边的尺寸 a/b/c/d**（师傅开孔要的就是这四个数） |
| `pickCrossPhase(...)` | **点状 + 长条通用**：选一个让分水线（或长条边缘）**压在砖缝上**的起铺相位 | 见 **4.4**（闭式，`X0 mod p` 一步算出；命中就是零骑线，不命中退化成小扫描找合法相位） |
| `drainCutPlan(drain, card, layout)` | **输出下料清单**：哪几块砖、怎么切、切完多大 | 按 2.4.3 的"落点判定"分支：砖心 → 单砖套割（给出 a/b/c/d 四个开孔定位尺寸）；四砖交角 → 回字对角切；长条 → 整排直切。分水线穿过的砖**不进这张清单**（改由 `rideReport` 报骑线）。<br>**返回值三笔账**：`drainCuts`（地漏周围）/ `edgeCuts`（靠墙收边）/ `ride:{count,max}`（骑线，不算切），外加 `ridgeCuts`（恒为 0，留作断言）/ `cutCount`（前两笔去重合计）。见 **7.15** |
| `drainAdvice(drain, card, tile, slope)` | **选型与校核建议**（纯提示，不改排版） | ① 按地面面积/短边推荐造型（2.4.3 第一步表）② 最远角抬高量 > 30mm 报警 ③ 长条排水量 vs 花洒流量 |

### 4.3 统一高度模型：`h = k ×（横向距离 + 竖向距离）`（本轮改的核心）

> 这一小节是整轮改动的地基。分水线从"对角线"改成"十字线"之后，
> 每个分区的高度该怎么给，必须重新定，否则会在分水线上出现台阶。

**公式**（就这么一句，纯算术）：

```
k = drop / 1000                       // 横、竖两个方向各自每毫米降多少
h(x, y) = k × ( |x − cx| + |y − cy| )  // 点状地漏，(cx,cy) 是地漏中心
```

展开到四个分区里，每个分区的 `h` 都是**线性**的，也就是一个平面：

```
左上角分区（x < cx, y < cy）：  h = k·(cx − x) + k·(cy − y)   → 平面 ✓
右上角分区（x > cx, y < cy）：  h = k·(x − cx) + k·(cy − y)   → 平面 ✓
……
四个分区的四个角高度一定共面（因为 h 是线性函数）
→ 所以分区可以直接存成"矩形 + 四角高度"，插值用双线性就够了
```

**为什么不写成"每个分区朝自己的对角方向斜"？**
那样看着更自然（水直奔地漏），但**两个分区在分水线上接不上**，会出现台阶：

```
地面 2000×1500，地漏在 (1000, 400)，drop = 10
沿横线 y=400 在 x=0 处：
   上左分区算出来  h = 10×1000²/1077 = 9.29mm   （1077 = 上左对角长）
   下左分区算出来  h = 10×1000²/1487 = 6.73mm   （1487 = 下左对角长）
                                    ↑
                          差 2.56mm —— 一道实打实的台阶，水就积在这
```

用上面那个统一公式就没有这个问题：

```
沿竖线 x=cx：h = k·|y − cy|   ← 左右两个分区算出来一模一样 ✓
沿横线 y=cy：h = k·|x − cx|   ← 上下两个分区算出来一模一样 ✓
```

**代价（必须说清）**：分区内部的最陡方向固定是 **45°**，不是严格指向地漏。
水从墙角出发，先斜着 45° 走到分水线（分水线其实就是一条"凹槽底"），
再顺着凹槽流到地漏。**不会积水，一定能流到地漏**——只是路径不是最短的那条。
这是"每个分区都能贴、接缝不出现台阶"必须付的价，见第 7 节。

**另一个代价**：墙角的总降量会比"理论圆锥漏斗"**大约 40%**。

```
地面 2000×1500，地漏正中，drop = 10
  本模型：墙角降 k×(1000+750) = 17.5mm
  理论圆锥：降 10×√(1000²+750²)/1000 = 12.5mm
```
换来的是"每块都是平面、切的都是直刀"。程序报"最远角抬高多少"时就用本模型的真值，
30mm 的报警阈值不变——大地面会更早触发，这是对的。

### 4.4 `pickCrossPhase`：让分水线压在砖缝上（闭式，本轮改的）

改成分水线之后，最大的好处是：**分水线是轴对齐直线，可以正好落在砖缝上**，
那就**一块砖都不用切**。而且这一步**不用扫描**。

```
输入：这条轴的长度 L、砖宽 t、缝 g、起铺模式、分水线（或长条边缘）位置 X0
输出：{ phase, zeroCut, minStrip }

1) p = t + g
2) ideal = ((X0 mod p) + p) mod p          ← 让分水线正好落在一条缝上的理论相位
3) 先试 ideal：
     用 layoutAxis(L, t, g, mode, ideal) 算一遍
     若两端边缘裁砖都 ≥ t/3  →  命中！zeroCut = true，本列/本排零骑线
4) 没命中（ideal 破坏了 1/3 老规矩）→ 退化成一次小扫描：
     在 [0, p) 里按 2mm 步进（约 150 个候选）找同时满足
        ① 边缘裁砖 ≥ t/3
        ② 切下来的条 ≥ minPiece
     的相位里，离 ideal 最近的那个（切下的条最宽）
5) 两条轴各跑一次，互不影响（分水线一横一竖，本来就是两条独立的轴）
```

**为什么这一步现在这么简单**：
对角线分水线是斜的，**永远**压不到直的砖缝上，只能靠扫描"少切几块"；
十字分水线是直的，`X0 mod p` 一步就能算出"要挪多少才压得上"。

**给师傅的输出要按"整列同一个位置"给**，这才是最省工的地方：

- 命中："十字分水线已压在砖缝上，**中间一块砖都不用管**；地漏孔另需切 **4** 块"
- 没命中："竖线这一列有 **5** 块砖骑在分水线上，**不用切**，底下最多垫 **0.5mm** 灰；地漏孔另需切 **4** 块"
  （**注意**：这里不能再说"每块切一刀"——2.5 之后中间一律不锯）

### 4.5 统一模型：四种造型 → 两个概念（新增）

前面所有函数之所以能复用，靠的就是这张收敛表。**下游（高度查询、裁剪、渲染）
只认"最低处形状"，不认 square / round / linear。**

| 造型 | 最低处 | 分区 `slopeZones` 输出（都是轴对齐矩形） | 分水线 `drainRidgeLines` | 洞口 `drainOpening` |
|---|---|---|---|---|
| 方形 square | 点 (cx,cy) | **4 块**：被 `x=cx`、`y=cy` 切出的四个矩形 | **2 条**：`x=cx` 整条、`y=cy` 整条 | 正方形（`size + 2×gap`） |
| 圆形 round | 点 (cx,cy) | 同上 4 块 | 同上 2 条 | 圆（本期按外接正方形近似，见 4.7 注） |
| 回字 concealed | 点 (cx,cy) | 同上 4 块 | 同上 2 条 | 正方形（`size + 2×gap`） |
| 长条 · 贴边 | 线段 [A,B] | **6 块**（3 段 × 2 侧）；**贴死墙时退化成 3 块** | **2 条**：过长条两端、垂直于长条 | 矩形（`len × wide`） |
| 长条 · 居中 | 线段 [A,B] | **6 块**（3 段 × 2 侧） | **2 条**：同上 | 矩形（`len × wide`） |

> **长条为什么也是 6 块而不是 3 块**：两端各一条分区垂线 → 横向 3 段；
> 长条自己那条线又把每段分成上下两半 → ×2 = 6。
> 只有长条**贴死墙**（离墙 0，墙那一侧高度为零）时，上面三块退化成零高被丢掉，才剩 3 块。
> （这是验算时发现的，方案初稿写"贴边 3 块"是错的，已改。）

**分水线怎么来的**（一句话）：
**过"最低处"的每个端点，作一条垂直于最低处、且平行于墙的线。**
点状地漏的最低处是一个点，过它作一横一竖两条 → 2 条线、4 块矩形。
长条的最低处是一条线段，过它的两个端点各作一条垂线 → 2 条线、3 或 6 块矩形。
**全部轴对齐，没有一条斜线**——这是本轮改动的全部意义。

长条两种情形的高度怎么定：

- **贴边**：`h = k × (到线段的横向距离 + 到线段的竖向距离)`，
  线段上的点 `h = 0`。中间那块是单坡（只有竖向在降），左右两块角区是 45° 双向降。
- **居中**：同样公式，中间被线段分成上下两块，各是单坡。

> 好消息：每个分区的高度都是**线性**的（4.3 的公式），
> 所以分区矩形的四个角**必然共面**，`slopeHeightAt` 用**双线性插值**就够了，
> 不用像上一版那样把四边形拆成三角形、也不用重心坐标。

### 4.6 `computeScene` 的接入点

在 `index.html:569` `computeScene` 这段里追加（**插在 596-624 行"门口发整砖 + 1/3 提醒"
之后、636 行开始进墙之前**）：

```text
scene.floor.slope = null
if (地漏开了 && 尺寸够 && 有地砖规格) {
    // ⓪ 先把造型归一化（四种 → 点 / 线段）
    const shape  = drainShape(drain, floorCard, doorEdge)  // { point|seg, open, isLine, drop }
    const zones  = slopeZones(floorCard, shape)   // 轴对齐矩形分区 + 四角高度（hc）
    const lines  = drainRidgeLines(floorCard, shape) // 一横一竖（或长条两端的垂线），全轴对齐
    const hole   = drainOpening(shape)            // 要挖掉的闭合轮廓（地漏口）

    // ① 调相位：让分水线压在砖缝上（闭式，门口那条轴不动 —— 见第 6 节）
    const phX = 门口锁的是 X 轴 ? floorX.phase : pickCrossPhase(…X 轴, 竖线位置 cx…).phase
    const phY = 门口锁的是 Y 轴 ? floorY.phase : pickCrossPhase(…Y 轴, 横线位置 cy…).phase
    if (phX 变了) floorX = layoutAxis(..., phX)   // 复用现成函数重算
    if (phY 变了) floorY = layoutAxis(..., phY)

    // ② 逐砖处理：分水线穿过的砖**不切**，只记"骑线 + 垫多少灰"
    //    真正动锯子的只有两处：地漏口（挖洞/切角）和靠墙收边（老规矩）
    const k     = drain.drop / 1000
    const rideX = rideReport(k, floorX, shape 的竖线 x)
    const rideY = rideReport(k, floorY, shape 的横线 y)
    const tiles = []
    for (每块砖的矩形 r) {
        const holed = cutRectByHole(r, hole)      // 挖掉地漏口；返回 null 表示没挖到
        const ride  = 该块是否骑线 ? rideGap(k, 砖宽, dL) : 0
        tiles.push({ rect: r, hole: holed, ride,
                     zone:  该块中心落在哪个分区,
                     tilt:  该分区的坡度方向 + 该块四角的高差 mm,
                     cut:   !!holed || 靠墙收边,     // 注意：骑线不算 cut
                     reason: holed ? "drain" : (靠墙收边 ? "edge" : null) })
    }
    scene.floor.slope = { drain, shape, zones, lines, hole, tiles,
                          phase:{x:phX, y:phY},
                          ride:{ x:rideX, y:rideY, max:Math.max(rideX.max, rideY.max) },
                          plan: drainCutPlan(drain, floorCard, {floorX, floorY}),
                          advice: drainAdvice(drain, floorCard, fDims, slopeInfo),
                          warnings:[...] }
}
```

**关键点：`scene.floor.x` / `scene.floor.y` 的结构完全没变**，只是相位值被换了一个。
所以：

- 老渲染路径（`paintTiles` 的两层 `fillRect`）**照旧能跑**，找坡关闭时画面与今天一模一样；
- 通缝锁定（`index.html:665-690`）里用的是 `floorAxis.phase`，**自动跟着走，零改动**；
- 错缝半砖偏移（`index.html:694-716`）同理，自动跟随。

### 4.7 出口清单要改

`return { deriveEdges, laidDims, compatible, layoutAxis, computeScene, nameOfEdge, computeRoomNet }`
（`index.html:824`）→ 已追加导出 **7 项已实现**（`drainToDiagram, drainShape, lowDist,
slopeZones, slopeHeightAt, rideGap, rideReport`），待实现后再追加 **7 项**
（`drainRidgeLines, drainOpening, cutRectByLines, cutRectByHole,
pickCrossPhase, drainCutPlan, drainAdvice`），共 14 项。
不删不改现有任何导出，老测试全绿。

（`pickCrossPhase` 一个函数同时管点状和长条，不再分 `pickRidgePhase` / `pickLinearPhase`
两个——因为改成分水线之后，两者的目标完全一样：让那条轴对齐的线压在砖缝上。）

> **一处近似要提前说**：圆形地漏的洞口本期按**外接正方形**处理
> （真正的圆弧套割要用 `arc()` + 多边形求交，放到第二期和"墙水管圆孔"一起做）。
> 误差只影响"孔的形状"显示，不影响坡面高度和切块判定；
> 师傅现场就是拿开孔器钻圆的，程序给出的是**直径 + 圆心定位尺寸**，够用。

### 4.8 界面改动（顺带说清，不写代码）

- `buildFloorCard`（`index.html:1190`）里加一个"地漏与找坡"折叠块，
  照抄 `featBlock`（`index.html:1045`）的写法：一个开关 + 几个数字框 + `_sync()` 回显。
  顺序：**开关 → 造型四选一（方 / 圆 / 回字 / 长条）→ 只显示该造型要填的字段 → 离左 / 离后 / 每米降几毫米**。
  - 选"长条"多出三项：长度、宽度、贴哪条边（贴边 / 房间中间）；
    再选一下花洒类型（手持 / 大顶喷 / 顶喷+手持）做排水量校核。
  - 选"回字"多出一项：切法（自动 / 单砖套割 / 回字对角切）。
  - **默认值按 2.4.1 的常见规格预填**：方 100×100、圆 φ100、回字 100×100、长条 300×64；
    留缝默认方 3 / 圆 5 / 回字 3 / 长条 3。
- `validateCard`（`index.html:884`）加的校验就是第 3 节末尾那五条
  （地漏轮廓必须在地面内且离墙 ≥ gap+20、长条尺寸范围、贴边留位、坡度 5~21）。
- 状态提示复用现有 `statuses`（`ok / warn / error` 三种），例如：
  - “分水线原要穿过 7 块砖，已为您挪动起铺点 34mm，**现在压在砖缝上，一块都不用管**”（点状，压上缝）
  - “有 **5** 块砖骑在分水线上，**不用切**，底下最多垫 **0.5mm** 灰”（点状，没压上缝）
  - “长条共压到 **2** 块砖，各切一刀（直切），切下的条最窄 **86mm**”（长条）
  - "地漏压在砖缝上，往左挪 68mm 落在四砖交角，可少切 2 块砖"
  - "最远墙角要抬高 53mm，超过 30mm，门槛和门扇都得跟着抬"（`error`）
  - "300 长条只够 35 L/min，大顶喷要 45，建议换 500"（长条排水量）

---

## 5. 渲染改动（三个视图各改哪）

### 5.1 新增四个共用的绘制原语（放在 `paintTiles` 旁边）

- `paintTilesSloped(ctx, floor, s, W, H)`：
  遍历 `floor.slope.tiles`，**没切的砖直接 `fillRect`**（2.5 之后绝大多数都是没切的），
  切了的砖按**多边形**用 `beginPath → moveTo/lineTo → closePath → fill` 画。
  颜色按该块所在坡面的朝向做明暗：`shadeColor(cssVar("--tile"), k)`
  （`shadeColor` 已经存在，`index.html:2088`），
  向阳的坡亮一点、背光的坡暗一点——**一眼看出地是四面歪的**。
- `paintDrain(ctx, shape, s)`：**按 4 种造型分支画**（新增重点）

  | 造型 | 怎么画 |
  |---|---|
  | 方形 square | 深色方口 + 虚线内框（虚线沿用 `dashedRect` 的做法）+ 中心小十字 |
  | 圆形 round | 深色圆口（`ctx.arc`）+ 虚线圆 + 中心小圆点 |
  | 回字 concealed | **双框**：外方框（实线）+ 内方框（虚线），中间填"地砖色"表示可嵌入的同款砖 → 一眼看出"隐形"效果 |
  | 长条 linear | 深色长矩形 + 里面画**几条平行短线**表示格栅面板 + 两端画小三角表示坡向 |

  标注统一用 `labelText`（`index.html:1470`）：
  - 点状三种：旁边标 `地漏 ▼0`
  - 长条：两端各标 `▼0`，中间标 `300×64`

  另外**洞口要按"挖掉"画**：被地漏压住的那块砖，只画外圈剩下的部分（L 形或多块），
  中间不填砖色——这样师傅看到的图和现场切完的样子一致。
- `paintRidges(ctx, lines, s)`：
  就画 **2 条**：一条横线、一条竖线，都过地漏、都平行于墙。
  用虚线、比砖缝略粗、用强调色，两端标"凹槽底 ▼0"。
  四个墙角标"最高 +17.5mm"之类（这个数就是 4.3 公式算出来的真值）。
  > 画成十字之后，图上和现场墨斗弹的线**一模一样**，师傅能直接对着图弹线——
  > 上一版画 4 条斜线，图上好看，现场根本照着弹不出来。
  > 注意：坡度**不靠箭头表示**。箭头只是示意，本方案的坡度是靠"砖被切开的形状 +
  > 明暗 + 四角标高数字"表达的，这是硬要求（约束 4）。
- `paintDrainPlan(ctx, plan, s)`：**把下料清单画在图上**（新增）
  在需要切的砖上，沿切口画一条红线，并标一个尺寸：
  - 单砖套割 → 标 `孔 106×106，中心离砖左 148 / 离砖上 148`（四个定位尺寸）
  - 回字对角切 → 每块砖标 `切角 45°，斜边长 74`
  - 长条整排 → 标 `本排不切` 或 `每块切下 62`
  - **骑线的砖不画红线**（它不用切），改画一条**细虚线 + 灰字**标 `垫灰 0.5`，
    跟"要动锯子"的红线区分开——别让师傅照着虚线去锯砖。
  风格沿用现有 `paintCuts`（`index.html:1376`）的红字 + 尺寸线，师傅不用重新学。

### 5.2 单墙立面图（`drawWall`，`index.html:1709`）

- 找坡关闭时：**完全不动**。
- 找坡开启时，该函数末尾新增一步 `paintSlopeFootLine(...)`：
  沿墙根画一条 **V 形折线**（两段直线，最低点在"地漏到这面墙的垂足"处），
  三个点各用 `labelText` 标高度（如"左 +17.5 / 中 +7.5 / 右 +17.5"）。
  **注意中间那个最低点不是 0**——它是 `坡度 × 地漏到这面墙的距离`，
  只有地漏本身才是 0。再画一条虚线水平线表示"按最高点起砖"的基准。
  目的：师傅一眼看到"这面墙底下有坡，底排砖要处理"。
  - **特例（长条）**：如果长条**平行于这面墙**，墙根高度**处处相等**，
    这条线是平的，直接跳过不画，并提示"本面墙墙根等高，底排砖不用处理"。
    只有长条**垂直**于这面墙、或是点状（十字分水）时，才需要画折线。

### 5.3 整屋展开图（`paintFloorNet` / `renderRoomNet`，`index.html:1766` / `1797`）

- `computeRoomNet`（`index.html:735`）**不用改**：它只算几何摆位，跟坡度无关。
- `paintFloorNet` 内部判断：`scene.floor.slope` 存在就调 `paintTilesSloped`，
  否则照旧调 `paintTiles`。因为 `renderRoomNet` 已经 `ctx.translate` 到地面矩形左上角，
  地漏和分水线直接画在地面矩形坐标系里即可，**不用改调用方**。
- 展开图不画每块砖的切块尺寸标注（手机上太乱，与现有"不标裁砖尺寸"的取舍一致），
  只保留砖形、明暗、地漏、分水线、四角标高。

### 5.4 三维整屋图（`buildRoomModel` / `makeFloorFace` / `renderRoom3DDraw`，`index.html:1885` / `2045` / `2138`）

- **本期做法（最小改动）**：
  `buildRoomModel` 生成地面贴图（离屏画布）时，改用 `paintTilesSloped` + `paintDrain`，
  再把这张贴图交给 `makeFloorFace` 原样贴上。
  → 3D 里地面仍是**平面**，但已经能看到"十字分水线、四块明暗不同的砖、中间有个地漏口"。
- **增强项（本期不做，写清路径）**：让地面真起伏。
  现在分区全是**轴对齐矩形**，这事比上一版简单多了：
  直接在地面网格上取 `N×M` 个小方格，每个顶点 `y = -slopeHeightAt(x, z)`，
  复用现成的 `project()` + `drawTexturedQuad`（`index.html:2019`）/ `drawTri`（`index.html:1996`）
  逐片贴即可，连"分区求交"都不用做。
  技术上是通的（纯算术、零依赖），但对"照着贴"帮助有限，排到后面。

### 5.5 各视图改动一览

| 视图 | 改哪个函数 | 改什么 |
|---|---|---|
| 单墙立面 | `drawWall`(1709) | 末尾加"墙根 V 形折线 + 三点标高"；长条平行于本墙时跳过 |
| 地面平面 | `drawFloor`(1720) | `paintTiles` → `paintTilesSloped`；加 `paintDrain` / `paintRidges` / `paintDrainPlan` / 四角标高 |
| 地面平面 | `paintCuts`(1376) | **只标"地漏口"和"靠墙收边"两种**（按现有红字标尺寸的风格标最短边）；骑线的砖不进这里，走灰字虚线 |
| 整屋展开 | `paintFloorNet`(1766) | 分支调用 `paintTilesSloped`（+ 地漏/分水线，不标尺寸） |
| 三维 | `buildRoomModel`(1885) | 地面贴图改用 `paintTilesSloped` + `paintDrain` |
| 新增 | `paintTilesSloped` / `paintDrain` / `paintRidges` / `paintDrainPlan` / `paintSlopeFootLine` | 5 个新绘制原语 |

---

## 6. 与现有规则怎么协调（冲突了听谁的）

| 现有规则 | 会不会打架 | 怎么算（优先级从高到低） |
|---|---|---|
| **门口发整砖**（`computeScene:596-615`） | 会：它锁定了一条轴的相位，坡度也想改相位 | **门口整砖赢**。被门口锁住的那条轴**不许动相位**（那条分水线就压不到缝上）；只允许调另一条轴。**所以最多只能做到"一条分水线压缝"，另一条只能让砖骑线**（不锯，垫灰）。提示里要说清楚：骑线不是切砖，别让师傅以为有砖要动锯子 |
| **墙地通缝**（`compatible` + `phaseOverride`，`index.html:665-690`） | 不会 | 墙的相位本来就取自 `floorAxis.phase`，我们只是把地面相位换成"被坡度调过的"，墙**自动跟着走**，一行不用改。这是本方案最省事的地方 |
| **墙地错缝（半砖偏移）**（`:694-716`） | 不会 | 同上，自动跟随 |
| **边缘裁砖 ≥ 1/3**（`layoutAxis:352`） | 会：让分水线压缝的那个相位，可能让边缘出现碎砖 | **1/3 规矩赢**：`pickCrossPhase` 里先试 `ideal`，一旦边缘裁砖 < 砖宽 1/3 就**放弃压缝**，退化成小扫描找一个合法相位（砖骑线就行，不追求压缝） |
| **骑线悬空量**（新规则，替代原来的"切下的窄条"） | 这是新引入的 | 分水线穿过整砖**不切**，改判悬空量 = `坡度 × 砖宽 ÷ 4`（2.5.1）。<br>**≤1.5mm** → 直接垫灰，不提示；**≤3mm** → 提示"建议调起铺点让分水线压缝"；**>3mm** → `warn`，建议降坡度或换小砖。<br>**注意这不是 1/3 规则**：1/3 管房间四周的收边，这条管屋子中间骑线的砖 |
| **地面中间的整砖不许切**（新增，硬规矩） | 会跟"沿线锯开"的老想法打架 | **不许切，一律骑线**。只有两处能动锯子：地漏周围 1~4 块（回字对角切/套割）、房间四周靠墙收边（1/3 老规矩）。见 2.5.3 |
| **墙压地（底排整砖）**（`:639-655`） | 会：墙根地面变成 **V 形折线**（两端高、中间最低，但中间那个最低点**不是 0**） | 三选一，**默认 B**：<br>**A** 底排砖跟着 V 形斜切（最好看、最费工，程序输出每块底砖两端的斜切高度）<br>**B（默认）** 按这条墙根的**最高点**（就是墙角那头）统一起砖，底下那道缝用踢脚线/收边条盖住，程序算出"最大空隙 XX mm"并提示<br>**C** 按最低点起（会露缝，**不推荐**，不提供） |
| **包管立柱柱脚**（`columnFootprints`，`index.html:1539`） | 轻微 | 柱脚仍按平面画，但提示"柱脚处地面高差 XX mm，现场需顺平"，本期不做几何处理 |
| **地漏落在砖哪** | 新引入 | 默认按**真实位置**算。同时**另算一套建议**：把地漏挪到最近的十字缝交点或砖心（通常 < 半块砖），对比能少切几块砖，显示提示"若地漏左移 87mm 落到砖缝交点，可少切 4 块砖"。**不自动改**——地漏位置是下水管定的，挪不动是常态 |
| **长条贴边 vs 门口发整砖**（新增） | 会 | 长条贴的那条边如果正好是门所在边 → **门口整砖赢**，长条相位让步，改为保证"切下的条 ≥ minPiece"，并在状态里说明原因 |
| **长条 vs 墙压地**（新增） | 看方向 | 长条**平行**于某面墙 → 那面墙根**等高**，底排整砖完全不受影响（长条最大优势之一）。长条**垂直**于某面墙 → 那两面墙按上面"墙压地"的默认 B 处理 |
| **回字对角切 vs 1/3 规则**（新增） | 不打架 | 回字切的是**砖的一个角**，不产生"边缘收边条"，**不受 1/3 规则管辖**。它受的是 `minPiece`：切角后剩下那块的最小边仍要 ≥ 50mm，通常远大于 |
| **长条居中把地面切成两半**（新增） | 会 | 双坡时地面被一条"谷线"分成两片，起铺仍按现有 `startMode`（居中/靠边）算，**不**因为长条就改成"从长条往两边发砖"。理由：从长条发砖会导致两侧都出现裁砖，师傅现场也不这么做 |
| **长条长度跨越多块砖**（新增） | 不打架 | 长条 `len` 通常 300~1524，会横跨好几块砖。判定按"每块砖是否与长条矩形相交"逐块做，天然支持任意长度，不需要特殊分支 |

---

## 7. 边界与近似（能做到什么，做不到什么）

### 能做到

- 高度**连续**：各分区沿十字分水线严丝合缝，走到哪都不会出现台阶（可断言，见第 8 节）。
- **分水线是轴对齐直线**（一横一竖），所以：
  - 每条线上骑线的砖，位置**整列 / 整排一致**，师傅一次抹灰就能处理完；
  - 调起铺相位常常能让**分水线正好压在砖缝上** → 中间一块砖都不用管；
  - 真正动锯子的块（地漏口、靠墙收边）切完还是**矩形**（不是斜的多边形）。
- 每一块砖的**最终形状**、**每一刀离砖边多少**、**切完两块各多大**——
  都能算出来，可打印成下料清单。
- 每一块砖的**高差**（四个角各高出地漏多少），能反推出"砖底要打多厚的灰"。
- 四种造型的**下料清单**：哪块砖切哪一刀、切完多大、开孔定位的四个尺寸。

### 做不到 / 是近似（明说）

1. **真曲面做不了，也不需要。** 圆锥漏斗每一点都精确指向地漏，但砖是平的，
   现实里贴不出来。本方案用若干个**平面矩形**近似它。
2. **每个分区内的最陡方向固定是 45°，不是严格指向地漏。**
   这是"分区是平面 + 接缝不出现台阶"的必然代价（4.3 有推导）。
   后果：水从墙角先斜 45° 走到分水线（凹槽底），再顺着槽流到地漏，
   **不会积水、一定能流到地漏**，只是路径不是最短的。
3. **地漏口内当水平处理。** 地漏面板需要平整安装，现实里口内就是平的。
   误差 ≤ `坡度 × 面板边长 / 2` = 1% × 50 = **0.5mm**，忽略不计。
4. **三个"坡度"数值别搞混**（这是十字分水线特有的）：
   - 横向每米降 = `drop`（靠尺沿墙量到的）
   - 竖向每米降 = `drop`
   - **朝地漏 45° 方向的真坡度 = drop × 1.414**
   程序给的三个数都要显示，别只报一个。
5. **墙角总降量比"理论圆锥漏斗"大约 40%。**
   `k×(dx+dy)` vs `k×√(dx²+dy²)`，两个方向都算满自然就多。
   程序报的"最远角抬高多少"用本模型真值，30mm 报警阈值不变。
6. **只支持一个地漏、且是一整段。**
   - 两个或两个以上地漏 → 要做"分水线网络 + 分区求交"，复杂度跳一档，本期不做。
   - 长条地漏的 **U 型 / L 型组合**（市面有拼接件）→ 本质是"多段最低线"，本期不支持。
   - 长条可以是**任意长度**（300~1524 都行），但必须是一段直线段。
7. **地漏必须平着放（横平竖直）。** 长条只能横放或竖放（`rot` 只有 0 / 90），
   不支持斜着放——斜放会让分水线变成斜线，回到"不好下刀"的老问题。
8. **圆形地漏按外接正方形近似。** 见 4.7 的注。真圆弧套割放到第二期。
9. **只支持矩形地面。** L 形、切角、圆弧边都不支持。
10. **不模拟水流。** 只能说"坡度方向和大小对不对"，不能算"水多久流干、会不会往门口跑"。
11. **不模拟砖的错台与砂浆厚度。** 相邻两块砖在分水线处会有几毫米的错台，
    程序只在 2D 里用明暗表现，**不输出每一块的砂浆厚度**（可留作后续）。
12. **地漏面板的高低不进高度模型。** 长条面板应装得比地面**低 1~2mm**、
    回字地漏中心应低于四周砖 **1~2cm**，这些是**安装数据**，程序只在提示里说，
    **不**把它算进坡面高度（否则会把整套坡面数值搞乱）。
13. **回字"嵌砖盖板"的同款砖不参与排版。** 面板中间那块嵌砖是地漏配件，
    不是地面砖网的一部分，程序只画个同色方块表示，不给它编砖号、不算裁切。
14. **"最远角抬高 > 30mm"是经验阈值，不是规范条文。** 各地做法不同，
    程序只报警不做决定，最终由师傅和业主定。
15. **"零切砖"这个说法已经作废**（本轮改）。
    分水线穿过整砖**一律不切**，改让砖骑线（2.5）。所以账要分三笔报：

    | 字段 | 含义 | 能不能是 0 |
    |---|---|---|
    | `drainCuts` | **地漏周围**要切的砖（回字对角切 / 套割） | **永远不能**，地漏总得开个孔 |
    | `edgeCuts` | **靠墙收边**要切的砖（1/3 老规矩） | 视房间尺寸而定 |
    | `ride` | **骑线**的砖数与最大垫灰量（**不是切**） | 压上缝就是 0 |
    | `ridgeCuts` | 因分水线而切的砖 —— **恒为 0**，留着给断言 8b / 14 用 | 必须是 0 |
    | `cutCount` | `drainCuts + edgeCuts` 去重合计（**不含 ride**） | ≥ `drainCuts` |

    程序报数时要分开说，不许写"零切砖"把洞口那几块也吞掉。
16. **分水线不能挪离地漏**（本轮改，重要）。
    高度公式里最低点就在"分水线 × 地漏"那个交点上，线一挪最低点就跟着跑。
    算例：地漏在 (1000,750)、洞口半宽 53mm，把线挪到 850 或 1150，
    最低点就跑到离洞口 **97mm** 的地方 → 水积在那儿。
    **能挪的只有砖缝**（调起铺相位），效果一样但不破坏排水（2.5.2）。
17. **骑线悬空量是个近似**。只算了"砖底到理想斜面的最大偏差"，
    没算砖本身的挠度、也没算砂浆的可压缩量。真实的做法就是靠师傅抹灰的手感，
    程序只负责告诉他"最多要垫几毫米"。

---

## 8. 验证方法（怎么算改对了）

以下每条都能直接写成 `test.js` 里的 `check(...)` 断言
（现有框架：`test()` 分组 + `check()` 断言 + `expect().toBeCloseTo()`，`test.js:24-37`）。
**先跑一遍现有全部断言做回归，再跑下面这些。**

| # | 断言 | 为什么它证明"改对了" |
|---|---|---|
| 1 | **回归**：`drain.on = false` 时，`scene.floor.x.segments` 与 `scene.floor.y.segments` 与改动前**逐段完全相等** | 保证没弄坏现有功能 |
| 2 | **高度连续（十字线上）**：沿竖线 `x=cx` 取 200 个点，每个点左右各偏 1mm 采两个样，断言 `|h左 − h右| < 0.01mm`；横线 `y=cy` 同理上下各采一次 | 证明没有台阶。这是本轮改动的**命门**——换成"各分区朝自己对角斜"的写法，这条会立刻挂（4.3 有 2.56mm 台阶的算例） |
| 2b | **分区四点共面**：对每个分区矩形，断言"第 4 个角的高度"== "用另外三个角确定的平面算出的高度"（误差 < 0.01mm） | 四点不共面就没法当平面贴，这条直接保证可施工 |
| 3 | **最低点唯一**：地面内撒 100×100 网格点，断言最小值出现在地漏点且 `h = 0`，且不存在 `h < -1e-6` 的点 | 证明地漏真的是最低点 |
| 4 | **坡度达标**：四角断言 `h(角) ≈ drop/1000 × (|dx| + |dy|)`（误差 < 0.5mm）；沿墙方向取样断言每米降 `drop`；朝地漏 45° 方向断言每米降 `drop × 1.414` | 三个方向的坡都要对得上（7 节第 4 条） |
| 5 | **排水方向**：每个分区内，最陡下降方向**恒为 45°**（指向地漏那个角）；沿分水线方向的坡度单调指向地漏 | 本模型是 45° 的，写死这条，将来有人改成别的模型会立刻被发现 |
| 6 | **切块没丢没重**：`Σ(所有切块面积) ≈ Σ(所有砖面积) − 地漏口面积`，相对误差 < 1e-6 | 多边形裁剪最容易漏块/重块，这条能一把抓住 |
| 7 | **十字线压缝 → 零骑线**：算例（地面 3020×1510、地漏 (1510,755)、砖距 302）断言 `rideReport(k, X, 1510).count == 0` 且 `max == 0`；同时断言 `plan.drainCuts == 4`（回字对角切）。**已用现有 `layoutAxis` + `rideReport` 跑通** | 压上缝就一块砖都不用管；同时防止"零切砖"把洞口那 4 块也吞掉 |
| 7c | **长条长度挑得对也能零切分区线**：`len` 取 604（= 2×302）时断言 `ridgeCuts == 0`；取 600 时断言 `ridgeCuts > 0` | 记住**最常见的 600/900 反而不行**，别想当然 |
| 7b | **压缝让位于 1/3 规矩**：同一个场景把地面尺寸改到让 `ideal` 相位出现 < 1/3 边缘裁砖，断言 `zeroCut` 变 false 且边缘裁砖回到 ≥ t/3 | 老规矩优先，别为了不切砖把边缘搞碎 |
| 8 | **骑线悬空量算得对**：断言 `rideGap(k, t, t/2) == 坡度 × 砖宽 ÷ 4`；`rideGap(k, t, 0) == 0`（折线在砖外不悬空）；`rideGap` 随 dL 从 0 到 t/2 单调递增 | 2.5.1 那条公式，别算错 |
| 8b | **地面中间不许有砖被切**（本轮改的命门）：断言 `plan` 里除"地漏周围"和"靠墙收边"之外**没有任何切块**（`t.reason` 只能是 `drain` / `edge`）；`plan.ridgeCuts` 恒为 0（见 7.15 的三笔账） | 一旦这里有砖要切，就退回到"地面中间锯砖"的老问题 |
| 8c | **悬空量分级提示对得上**：≤1.5mm 无提示、≤3mm 提示压缝、>3mm `warn`。取 4 组（砖宽 × 坡度）逐一断言等级 | 别把 0.75mm 的小事报成 error 吓唬人 |
| 9 | **通缝跟随**：开启找坡 + 墙设 `alignEdge` 后，断言墙的 `entry.x.phase` 等于"调过后的"地面相位（按左右/后墙的镜像规则） | 通缝没被坡度破坏 |
| 10 | **门在四边**：`doorEdge` 分别取 bottom/top/left/right，断言 `drainToDiagram` 换算出的 (x,y) 落在预期象限 | 地漏别标错位置（最容易出 bug 的地方） |
| 11 | **极端输入不炸**：地漏正好压在砖缝上 / 正好在墙角 / 正好在正中 / 正好贴着墙（分区宽度为 0）/ `drop = 0` / 地面比一块砖还小 → 不出现 NaN，切块数有上界（< 4 × 砖数） | 防止除零、死循环。特别注意"地漏贴墙"会让某几个分区宽度为 0，要能安全跳过 |
| 12 | **性能**：8m × 6m 地面 + 300mm 砖，整段找坡计算 < 50ms（`Date.now()` 掐表） | 手机上不能卡。改成分水线后**比上一版快很多**（闭式求解，不用扫 150 个候选） |

**造型相关（2.4 新增）：**

| # | 断言 | 为什么它证明"改对了" |
|---|---|---|
| 13 | **四种造型都能跑通**：`kind` 分别取 square / round / concealed / linear，各自生成的 `zones` 非空、`tiles` 非空、无 NaN。**点状断言 `zones.length == 4`**；**长条贴边/居中都断言 `== 6`**；**长条贴死墙（fromBack=0）断言 `== 3`** | 4.5 的收敛表没写错。（这部分已由"三·补"那组直接调用验收） |
| 14 | **长条只切直刀、切得极少**：断言 300 长条 `plan.ridgeCuts === 0`（分区线不切砖）且 `plan.drainCuts ≤ 3`；`plan.minStrip ≥ 50`；切块都是轴对齐矩形 | 证明 2.4.4 的三条优势（直刀 / 极少 / 不窄）真的成立 |
| 14b | **长条本体压住的砖必切**（反向断言，防止把话说满）：任意 `wide ∈ [25,100]`、`len ∈ [300,1524]`，断言 `plan.drainCuts ≥ 1` | 分水线可以零骑线，但长条要占掉一条地。**切砖数是三笔账（drain / edge / ride），别混成一个数** |
| 15 | **长条单坡等高**：长条平行于 X 轴贴下边放，断言沿"平行于长条"的方向取 10 个点，高度**完全相等** | 证明"长条平行的那面墙墙根等高"（6 节那条规则） |
| 15b | **点状墙根是 V 形**：断言沿任一面墙取样，高度是"两端高、中间低"的两段直线，且中间最低点 = `drop/1000 × 地漏到该墙的距离`（**不是 0**） | 5.2 画的就是这条线，画错师傅会按错尺寸下料 |
| 16 | **洞口不跨界**：断言地漏外轮廓（含 `gap`）完整落在地面矩形内，且离每面墙 ≥ `gap + 20` | 地漏压到墙上＝装不下 |
| 17 | **套割不骑缝**（点状）：当落点判为"砖心"或"四砖交角"时，断言孔的四边到最近砖缝的距离 ≥ `gap + 20`。不满足则 `plan.cutStyle` 必须切成别的方案并给提示 | 毛边露在砖缝上＝最难看也最容易崩 |
| 18 | **切法与落点一致**：`cutStyle:"auto"` 时，断言落在砖心 → 出 `hole` 方案（1 块砖）；落在四砖交角 → 出 `corner` 方案（4 块砖）；压缝 → 出 2 块直切 | 2.4.3 的落点判定表和代码对得上 |
| 19 | **开孔定位尺寸自洽**：`hole` 方案给出**孔中心到砖四边**的四个定位尺寸 `a/b/c/d`，断言 `a+b = 砖宽`、`c+d = 砖高`（误差 < 0.5mm）且都 > 0；落砖心时还应 `a≈b`、`c≈d` | 师傅拿这四个数去开孔，算错了就废一块砖 |
| 20 | **抬高量校核**：最远角抬高 = `drop/1000 × (|dx| + |dy|)`，断言与 `slopeHeightAt` 在该角返回的值一致（误差 < 0.5mm）；抬高 > 30mm 时 `advice` 里有 `error` | 2.4.5 第 1 条 |
| 21 | **长条排水量校核**：按 `len` 查表（300→35、500→45、600→55 L/min），花洒需求取 hand 12 / rain 25 / both 40。断言 `300+both`（35 < 40）**给提示**、`300+hand`、`600+both` **不给提示** | 2.4.5 第 2 条 |
| 22 | **长条任意长度**：`len` 取 300 / 600 / 900 / 1524，断言涉及砖数随长度单调递增，且都不炸 | 长条横跨多块砖不用特殊分支 |
| 23 | **长条贴边不越界**：中心线到墙距离 < `wide/2 + 20` 时，`drainAdvice` 给 `error`。（`validateCard` 在前端、不在引擎块里，`node test.js` 测不到，所以这条判据走 `advice`） | 地漏槽体要有地方放 |
| 24 | **分水线只有 2 条且都轴对齐**：断言 `drainRidgeLines`（原名 `drainCutLines`）返回长度 == 2，且每条线要么 `x1 == x2`（竖线）要么 `y1 == y2`（横线） | 本轮改动的**第三个命门**：一旦返回 4 条斜线，就退回到"现场没法下刀" |
| 25 | ~~同列砖切在同一位置~~（已作废：地面中间不切砖了） | 改成断言 7 的"零骑线" |
| 26 | **骑线砖底下要垫多少**：把骑线的砖按 dL 分组，断言每块报出的 `gap == rideGap(k, 砖宽, dL)`，且全都不超过阈值 | 师傅照着这个数抹灰 |
| 27 | **分水线不许挪离地漏**（本轮改的命门）：把线从 1000 挪到 850/1150，断言最低点跟着跑到离洞口 97mm 处（地漏处高度 > 0） | 防止有人"图省事把线挪到砖缝上" |

再加**三条人工验收**（写不了断言，但必须看一眼）：

- **（最重要）找一个 2m × 2.5m 的小卫生间实例，把图纸给师傅，让他按图弹十字线。**
  弹出来的线必须跟地漏对齐、平行于墙；线上的砖要么**压在缝上**（不用管），
  要么**骑线**（垫灰，不锯）。
  师傅看完说"这线能弹、屋子中间的砖不用锯"，这方案才算过。
- 图上标的分水线位置，用卷尺按"离左 / 离后"量一遍，误差 < 5mm。
- 四种造型各出一张图：**方形/圆/回字**看得出"孔在哪块砖、怎么切"，
  **长条**看得出"一共就切这两三块"。师傅看不懂就是没做对。
- 找一个**分水线压缝**的实例（如 3020×1510 地面、300 砖、地漏在 (1510,755)），
  打印出来现场对照：十字分水线应正好盖在砖缝上，中间一整列砖**一块都不用锯**。
  再找一个**压不上缝**的实例，确认程序报的是"骑线 + 垫灰几毫米"，**不是"切几刀"**。

---

## 9. 第二期扩展（马桶口 / 蹲便口 / 墙水管口怎么纳进来）

一句话：**把"地漏"抽象成"洞口（opening）"的一种，三类共用同一条流水线。**

- **数据**：地面加 `floor.fixtures:[{ type:"toilet"|"squat", x, y, w, h, rot, pitDist }]`
  （`pitDist` = 坑距，排污口中心离墙的距离，师傅最关心的数）；
  墙面加 `wall.pipes:[{ x, y, d, kind }]`（离左 / 离地 / 管径 / 冷热水）。
- **裁剪**：洞口是**闭合轮廓**（矩形或圆），分水线是**开放直线**。
  好消息是 **2.4 已经把"矩形洞口"这条路打通了**（`drainOpening` + `cutRectByHole`，
  长条地漏就是这么处理的），所以马桶口这种矩形洞**直接复用**即可。
  剩下要补的是**真圆弧**：`cutRectByHole` 扩成 `cutRectByShape(rect, lines, holes)`，
  顺着把"圆形地漏"和"墙水管圆孔"一起做掉（本期圆形是按外接正方形近似的，见 4.7）。
- **渲染**：`paintTilesSloped` 画完之后，按现有"门洞/窗洞"的画法
  （`paintWallNet`，`index.html:1772-1790`：色块 + `dashedRect` 虚线 + 标注）
  画洞口，并标师傅要的两个数：马桶标"坑距 305"，水管标"φ25，中心离地 450、离左 300"。
- **顺序**：建议先做**墙水管圆孔**（只影响一面墙的渲染，不碰地面找坡主干），
  再做**马桶口**（要动地面裁剪主干，但矩形洞已复用现成的 `cutRectByHole`）。
- **再往后（第三期）**：长条地漏的 **U 型 / L 型拼接组合**——
  本质是"多段最低线"，需要把 `slopeZones` 从"一条线段"扩到"折线/多段线"；
  以及**两个地漏**的分水线求交。这两件事是同一个问题，放一起做。

---

## 附录 A：质量自检对照

| 项 | 是否满足 | 落在哪 |
|---|---|---|
| **A 针对性** | ✅ | 单地漏（每节都按"只有一个"写，第 7 节明说多地漏不做）；坡度真影响排版（2.3 列出四处真实变化：起铺点、砖底下垫灰、**整列同一位置压缝/骑线**、墙砖底口，2.4 再加"造型决定切法与切几块"）；只给方案（全文无最终代码，只有伪代码与算式）；零依赖（4.2 每个算法都注明"纯算术"，无库无迭代求解） |
| **B 不回避硬骨头** | ✅ | 1.2 列出三个躲不开的冲突（分水线穿砖 → 改骑线、骑线悬空量、墙脚 V 形），并用对照表说清"为什么不能用对角分水线"；1.3 给出 6 条取舍；4.3 用 2.56mm 台阶算例证明"必须统一高度模型"；2.5.2 用 97mm 积水的算例证明"分水线不能挪离地漏" |
| **C 零依赖可行** | ✅ | 核心算法退到最简：高度就是 `k×(｜dx｜+｜dy｜)`；分区是轴对齐矩形、双线性插值；骑线悬空量是一条 `k×dL×dR/t` 的乘除；剩下的切割只有"地漏口挖洞"和"靠墙收边"两种，都是轴对齐矩形（一元一次求交，或 `layoutAxis` 现成就有）；相位是 `X0 mod p` 闭式求解。无矩阵库、无优化求解器、连三角函数和重心坐标都不需要了 |
| **D 可理解性** | ✅ | 2.2 六步现场贴法（明确"地面中间一律不切，骑线的砖底下多抹灰"）；"倒扣的小屋顶"比喻；术语小抄表；2.4 用 ASCII 图讲清"点状 vs 线状"和"地漏落在砖的哪个位置"；4.3 用具体数字讲清为什么不能各分区各自为政 |
| **E 模块清晰** | ✅ | 第 3 节列字段，第 4 节列函数与 `computeScene` 接入点，第 5 节按视图列到具体函数名与行号；四种造型差异全部收敛到 4.5 一张表 |
| **F 边界与近似** | ✅ | 第 7 节 **17 条**，明确"真曲面做不了、最陡方向固定 45°、三个坡度数值别混、墙角降量比圆锥大约 40%、圆形按外接方近似、地漏必须横平竖直、只支持矩形地面、只一个地漏且不分段、不模拟水流、**骑线悬空量只是近似**、**分水线不能挪**" |
| **G 可验证** | ✅ | 第 8 节 **33 条**断言思路 + 4 条人工验收。其中 2、2b、7、7c、8、8b、8c、24、26、27 是"命门断言"；**算例 7（零骑线）、8（悬空量公式）、27（分水线不能挪）已用现有 `layoutAxis` + `rideGap` 在 `test.js` 里跑通**（137 条通过 0 失败），不等引擎落地就能锁死算术 |
| **H 造型与地形** | ✅ | 2.4 给出四种造型的真实规格与四种裁砖切法；2.4.3 按卫生间面积/砖规格/是否淋浴房给出选型与"落点→切几块"判定；2.4.5 加抬高量与排水量两条真实尺寸校核 |
| **I 现场可操作** | ✅ | 分水线轴对齐 → 弹线、压缝都能落到图纸上；1.2 用对照表逐条比"对角 vs 十字"；2.2 六步贴法明确"地面中间不切砖、骑线砖底下多抹灰"；人工验收第一条就是"让师傅按图弹线，弹得出来才算过" |
| **J 不许地面中间锯砖（本轮改的核心）** | ✅ | 2.5 整节：给出骑线悬空量公式 `坡度 × 砖宽 ÷ 4` 与四档数值表、分水线不能挪的 97mm 反例、只允许在"地漏周围 + 靠墙收边"动锯子；1.3 / 6 / 7 / 8 各处同步改掉"沿线锯开"的说法；测试里 8b 直接断言"没有任何一块是因为分水线而被切" |

## 附录 B：改动点清单（按文件行号）

| 位置 | 改动 |
|---|---|
| `index.html:279-830` 引擎块 | **已落地 7 个**：`drainToDiagram`(380) / `drainShape`(402) / `lowDist`(453) / `slopeZones`(472) / `slopeHeightAt`(515) / `rideGap`(546) / `rideReport`(555)，插在 `layoutAxis`(312) 与 `computeScene`(569) 之间，**共 +176 行**。待实现 7 个（见 4.2 表）。导出列表已加 7 项（824）。**注意：`pickRidgePhase` / `pickLinearPhase` 已合并为 `pickCrossPhase` 一个；`drainCutLines` 应改名 `drainRidgeLines`（分水线不切砖了）** |
| `index.html:312` `layoutAxis` | **不改** |
| `index.html:569` `computeScene` | 在 596-624（门口发整砖 + 1/3 提醒）之后、636（进墙）之前插入找坡段，见 4.6 |
| `index.html:735` `computeRoomNet` | **不改** |
| `index.html:840-853` `defaultState` | 地面卡片补 `drain` 默认对象（含 `kind:"square"` 及四种造型的常用默认尺寸） |
| `index.html:875` `parseMM` | **不改**（坡度用"每米降几毫米"的整数，正好复用） |
| `index.html:884` `validateCard` | 加第 3 节末尾那五条地漏校验（含长条尺寸范围、贴边留位、离墙距离） |
| `index.html:1190` `buildFloorCard` | 加"地漏与找坡"折叠块：造型四选一 + 按造型显示字段（仿 `featBlock`，1045） |
| `index.html:1343` `paintTiles` 旁 | 新增 5 个绘制原语：`paintTilesSloped` / `paintDrain`（四种造型分支）/ `paintRidges` / `paintDrainPlan` / `paintSlopeFootLine` |
| `index.html:1709` `drawWall` | 末尾加 `paintSlopeFootLine`（长条平行于本墙时跳过） |
| `index.html:1720` `drawFloor` | 分支调用 `paintTilesSloped`；加 `paintDrain` / `paintRidges` / `paintDrainPlan` |
| `index.html:1766` `paintFloorNet` | 分支调用 `paintTilesSloped`（不标尺寸） |
| `index.html:1885` `buildRoomModel` | 地面贴图改用 `paintTilesSloped` + `paintDrain`（离屏画布在 1925） |
| `index.html:2088` `shadeColor` | 不改，直接复用做坡面明暗 |
| `test.js` | 已加"三·地漏与找坡"组（直接调用，137 通过 0 失败）+ 造型/命门契约骨架（等 7 个函数落地后自动启用） |
