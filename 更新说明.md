# 排砖预览 · 更新说明

本说明记录本次对「排砖预览」网页（卫生间墙地砖排版工具）做的功能更新。
所有数据都保存在本机浏览器，刷新页面即用，不用联网。

---

## 一、门口发整砖
- 选好门所在的那条边后，地面铺砖会 **从门口开始排整砖**，零头裁砖自动甩到对面的墙。
- 看板会提示「地砖从门口（X边）发整砖」；如果对面的裁砖小于 1/3 砖，会红字提醒「远端裁砖 < 1/3，请现场定夺」。

## 二、包管立柱：可显示、可改、可删
- 点「＋ 添加立柱」会直接在卡片里加一行（不用弹窗，手机浏览器也能用），在行内填 **宽、深** 即可。
- 每根立柱的 **离左 / 宽 / 深** 三个框都能直接改，改完图立刻重绘。
- 每行末尾有 **✕ 删除按钮**，确认后即可删掉该立柱及其包住的管道。

## 三、立柱自动贴墙角（动态吸附）
- 每根立柱可切换 **自由 / 贴左角 / 贴右角**：
  - 贴左角：离左自动算成 `0`；
  - 贴右角：离左自动算成「墙宽 − 立柱宽」。
- **以后你改了墙宽，立柱会自动跟着贴角**，离左不用重填。
- 贴角时「离左边」框变灰只读（数值是自动算的）；选「自由」就能自己填离左。
- 贴角模式下不再报错「三个数都要填正整数」，只校验宽和深。

## 四、非整砖标注实测尺寸
- 图上凡是 **裁砖（不是整砖）的地方**，会用 **红字标出它的实际尺寸（毫米）**：
  - 横向裁砖条 → 标宽度；
  - 纵向裁砖条 → 标高度。
- 地面和每一面墙都生效，方便现场照着下料。

## 五、图例
- 页面标题下方新增一行图例，方便业主一眼看懂：
  - 🟥 红字数字 = 裁砖实测尺寸
  - 🔵 蓝块 = 方位（前 / 后 / 左 / 右）
  - 🟫 棕块（两侧压暗）= 包管立柱（往外凸）
  - ⬜ 白虚线框 = 门 / 窗

## 六、修复
- 修复了「包管立柱」参数区偶尔不显示的问题：现在该栏默认展开、稳定渲染。

## 七、立柱"往外凸"（2026-08-26 更新）
- 立柱是包着管道凸出墙面的墩，不是凹进去的洞：
  - 平面示意图里改用 **实线框 + 两侧压暗、中间提亮** 的画法，和门/窗的虚线框（凹洞）区分开；
  - **三维整屋图里立柱真的凸出墙面**：按你填的「深」朝屋里凸出一个立体盒子，正面亮、侧面暗。
- 修复手机浏览器（尤其 iPhone）点「＋ 添加立柱」没反应的问题：不再用弹窗输入，改为直接在卡片里加行填尺寸。
- 刚添加、还没填数字的立柱不会马上报红错。

## 八、立柱压地：地面图考虑柱脚、三维图加落地阴影（2026-08-26 更新）
- 立柱是落在地面上的墩子，地砖要围着柱脚裁切：
  - **地面图**：柱脚位置画棕色"柱"色块，压住下面的地砖；被柱边穿过的砖格，用红字标出"砖缝到柱边"剩余块的实测尺寸，现场照标裁切；红字标签如果互相重叠会自动推开，保证看得清；
  - **整屋展开图**：地面同样画出柱脚色块（不标红字，保持干净）；
  - **三维整屋图**：柱脚在地面上投一块**与柱脚面积完全重合**的阴影，柱子像真的坐在地上。
- 柱脚位置按立柱的「离左 / 宽 / 深」和墙贴的边自动推算；带门的墙左右镜像，柱脚跟着镜像，三张图位置一致。
- 柱边恰好落在砖缝上 = 不用裁，不标红字。

## 九、三维图砖缝固定可见宽度、改中性灰（2026-08-26 更新）
- 三维整屋图里砖缝原来只有约 0.35 像素（2mm × 贴图分辨率），几乎看不见，墙面/地面看着像一整块纯色，分不清砖的大小。
- 新增「砖缝最小像素宽」`R3D_MIN_GROUT`（默认 2px）：砖缝再窄也至少画 2px，能直接看出砖的尺寸。
- 砖缝颜色从纯白改成中性灰（新增 `--r3d-joint`，浅色 #8B8D90、深色 #2D3138）：避免大房间低分辨率下出现一片亮白格子，看着不刺眼；二维平/立面图的砖缝仍是白色，不受影响。

## 十、裁砖规则按规范收紧（JGJ/T 304-2013）

参照《住宅室内装饰装修工程质量验收规范》JGJ/T 304-2013 及行业惯例，排砖逻辑做了以下收紧：

- **边缘裁砖不小于 1/3 砖（规则 1）**：墙/地铺砖默认居中铺法，遇到非整砖时，把不足一段的零头**平分甩到两端**（两侧对称），且每段裁砖宽度都保证 **≥ 1/3 砖**。若某侧裁砖仍小于 1/3 砖（房间尺寸卡得特别死），画面会**黄字提醒**「边缘裁砖 < 1/3，请现场定夺」。
- **每面墙裁砖不超过 2 排/列（规则 4）**：墙宽方向或墙高方向若出现 **超过 2 段裁砖**，会黄字提醒「裁砖超过 2 列 / 2 排，建议调整砖规格或起铺」，避免碎砖太多不好看也难贴。
- **墙砖压地砖（规则 5，可开关）**：卫生间墙面最底排（贴地那排）强制是**整砖**，裁砖甩到**顶上**藏起来。对应设置项「墙砖压地砖（最底排贴地整砖，裁砖甩到顶）」默认勾选；想关掉就取消勾选。
- **门窗洞口对齐整砖边界（规则 6，近似）**：墙上开了窗时，会按窗台高 + 窗高**反推一个起铺起点**，让**窗洞上沿尽量落在一条砖缝上**（窗顶不骑在半块砖中间）。
  - 这是**近似做法**：它只把窗顶对齐到整砖边界，并不对窗做左右对称的整砖重切；若窗宽方向也需要严格对称，请现场师傅按图微调。
  - 门按「从门口发整砖」处理（见第一节），门洞边缘同理尽量落在砖缝上。
- **不通缝也优先"错缝"对齐地面（规则 7）**：某面墙选了"不通缝"（不锁到具体某条地面边）时，不再完全自由居中，而是**优先把墙砖缝对齐到地面砖缝的"另一条"——即半砖偏移**，让墙砖缝与地面砖缝**错开**（不直上直下），更美观也符合铺贴习惯（像 A 墙锁了通缝，B 墙不通缝时就对到地面错开的缝上）。
  - 仅当这种半砖偏移会让墙两端裁砖 **< 1/3 砖**时，才回退到原来的自由居中铺法；状态栏会显示「已与地砖错缝对齐（半砖偏移，墙地缝错开）」。
  - 三维整屋图与此完全一致（不通缝时同样采用这条错缝排版）。

> 说明：规则 6 为洞口边缘对齐的近似，非完全对称重切；规则 7 的"错缝"为半砖偏移对齐，非逐砖错位；最终下料仍以现场实测为准。

## 十一、公共角部立柱：贴墙角的立柱两面墙都显示

- 卫生间墙角是**两面墙共用**的。某面墙的立柱若"贴左 / 贴右"（即压在墙角上），它其实也是相邻那面墙的墙角立柱。
- 现在这类**角部立柱会被当作"公共立柱"**：在它所属的**两面墙**上都会画出来（墙面图、整屋展开图、三维整屋图都一致），不用在两面墙各加一遍。
  - 例如 **A 墙（前）贴左 与 C 墙（左）贴右是同一个角柱**：在 A 墙加了之后，C 墙自动也显示；反过来 C 墙贴右、A 墙也会显示。
- 俯视地面图仍只画**一份**柱脚（地面阴影也只一份），不会出现重叠的两块。
- 仅"贴左 / 贴右"的角部立柱会共享；自由定位（填了具体"离左"数值）的立柱仍只属于所在那面墙。

## 十二、修复：三维图不跟随"通缝/不通缝"设置

- 之前三维整屋图在生成每面墙贴图时，**不管这面墙选的是"不通缝"还是"通缝"，都强制把砖缝锁到地面相位**，导致把某面墙改成"不通缝"后，整屋展开图更新了、三维图却没变。
- 现已修正：三维图读取该墙自己的 `alignEdge` 设置——
  - 选"不通缝"（`none`）：三维直接采用和整屋展开图一致的自由排版；
  - 选"通缝"（前/后/左/右）：三维才把砖缝锁到对应的地面边，且左/后墙按贴边反向镜像，**与整屋展开图完全一致**。
- 顺带修正：三维里左墙/后墙（非门墙）的镜像对齐原来误用了“是否门墙”判断，现已统一为按 `alignEdge` 判断，三维与展开图对缝更准。
- 修复（立柱/门窗朝向）：左墙、后墙（门墙）在三维里的朝向与整屋展开图相反（展开图里左墙 `rotate(-90°)`、后墙 `scale(-1,-1)`，都把“离左=0”画到后端），若不做镜像，**左/后墙的立柱、门洞会在三维里跑到墙的相反一侧**。现已对**左墙、后墙（门墙）**做水平镜像，三维墙面的立柱/门窗位置与整屋展开图对齐。
  - 注意：**右墙不要镜像**。右墙在展开图里是 `rotate(+90°)`，“离左=0”在展开图里落在墙的**前端角**，而三维里贴图 `u=0` 本来就锚在前端角，方向已经一致；若再镜像，右墙的砖缝、立柱会相对整屋展开图整体左右反过来（见第十四节 D 墙问题）。

## 十三、使用小提示
- 所有改动 **自动保存** 在本机浏览器，关掉再打开还在。
- 想给业主看效果：手机截图发微信即可。
- 代码已放到 GitHub（公开仓库）：<https://github.com/niehuanhua/tile-preview>

## 十四、修复：D 墙（右）三维砖缝与整屋展开图镜像错位

- 现象：把某面墙设为“右（D 墙）”并通缝后，整屋展开图与三维整屋图里这面墙的**砖缝（以及立柱）左右反过来对不上**——展开图里靠前端角的缝，在三维里跑到了后端角。
- 原因：上一节的左/后墙镜像修正被**一并套用到了右墙**。但右墙在展开图里是 `rotate(+90°)`（不是镜像旋转），它“离左=0”那端本来就落在墙的**前端角**；三维里右墙贴图 `u=0` 同样锚在**前端角**，两者方向已经一致。再对右墙做水平镜像，就把整面墙的砖缝/立柱相对展开图整体翻了一次。
- 处理：三维图只对**左墙、后墙（门墙）**做水平镜像，**右墙、前墙不做镜像**。这样右墙（D 墙）的砖缝、立柱与整屋展开图、2D 立面完全一致；左墙/后墙的立柱/门窗仍保持正确朝向。
- 验证：用引擎算出的 D 墙缝位置，展开图里距前端角分别为 `0/188/990/1792/2594 mm`，三维图（右墙不镜像）同样得到 `0/188/990/1792/2594 mm`，完全吻合；若错误地镜像则变成 `3000/2812/2010/1208/406 mm`（整条缝反了）。

---

## 十五、底排墙砖：按地面最低点起、高的地方裁掉（2026-09-10）

- **底排不再"按墙角最高点起 + 踢脚线盖缝"**。原来那句话只是状态栏的一句提示，墙砖实际还是按"地面是个平面"排的；现在真的按地面来排了：
  - **底排上沿** = 一块砖高 + **全屋墙根最低点**，四面墙的底排上沿都落在**同一条水平线上** → 墙角处的横缝对得齐（这是选定做法，代价是墙根较高的那面墙底排几乎每块都要裁）；
  - **底排下沿**贴着墙根坡线：地面高的地方把砖裁短（裁掉多少 = 那处地面比全屋最低点高出多少），最低的地方正好是一块整砖；
  - 图上**每块底砖标出实际高度**（红字），师傅照着裁：
    - 普通的一块下沿是**一条斜线** → 标"左/右"两个数；
    - 正好**骑在墙根最低点上**那块，下沿是**人字形折口**（中间最高、两头低）→ 标"左/中/右"三个数，不然照两端直裁会在中间缺一块。
- **超差提醒**：底排最矮处不足 1/3 砖高（和"边缘裁砖 ≥ 1/3"同一条老规矩）→ 黄字提醒"请现场定夺"；墙太矮、底排上沿放不下 → 红字报错。
- **三个视图**：单墙立面与整屋展开图的墙底都按坡线裁掉（展开图不标尺寸，保持干净）；**三维图不变**——三维地面是一张"平贴图"，墙底按坡裁会和水平地面之间露出一条缝，所以三维仍是近似。
- **没找坡时一切照旧**：关掉地漏找坡，墙砖竖向排版还是老规矩（底排整砖、裁砖甩到吊顶侧），每面墙的相位与改动前**完全相同**。
- **窗顶让步**：找坡开着时底排上沿必须落在全屋统一那条线上；某面墙的窗顶边若对不上砖缝，优先保底排对齐，并在状态栏说明"窗顶这排让步"。

## 十六、地面新增「蹲便器」：砖围着它裁 + 当找坡最低点（2026-09-10）

- **地面卡片多了「蹲便器」折叠块**，填的全是现场拿卷尺量得到的数：
  - **排污管中心**离左墙 / 离后墙（和地漏同一套量法）；
  - **便器后背靠哪面墙**（前 / 后 / 左 / 右）；
  - **便器靠墙那条边离墙多远**（行业惯例约 150mm，免得碰冲水设备）；
  - 便器外形（长×宽，常见 520×420 / 535×430 / 580×450 一键选）、砖与便器的留缝、坡度。
- **坑距自动算好给你看**（= 排污口中心到所靠那面墙的距离，常见 300~450）。"排污口在便器本体上离边多少"行业里没有统一值，所以程序**不猜**：**排污口要是落在便器外面，直接红字报错**（典型情况：把前排水便器装到后排水的管位上）。
- **砖围着洞口裁**（嵌入式留坑位）：洞口 = 便器外形 + 2×留缝；洞口范围内的砖**真的被挖掉**，周边砖用红字标出"砖缝到轮廓边"的实测尺寸，师傅照着裁。
- **排污口当找坡最低点**：四面墙根照旧按"到排污口的距离"抬高，两条十字分水线照旧。**地漏同时开着也不打架**——地漏仍按洞口裁砖，只是不再当最低点（状态栏会写明当前谁是最低点）。
- **三个视图都画**：地面图上画便器轮廓（冷灰蓝色）和排污口（和地漏一个画法），并标「坑距 XXX」；整屋展开图和三维地面贴图里也画轮廓（不标尺寸，保持干净）。

> 三维仍是"平地面近似"：蹲便器的下沉凹坑在地面以下，三维里只体现为地面贴图上的一块色，不做立体起伏。

## 十七、新工具：全屋地板砖「通铺」（house.html）（2026-09-11）

> 这是**另一个独立工具**：`house.html`（双击打开，用法与现有工具一样）。
> 现有的 `index.html`（卫生间墙地砖）**一行都没动**，两个工具存档互不干扰。

**它解决什么**：全屋通铺——**一块砖、一张网铺满全屋**，砖缝从客厅一直对到卧室，门口不断缝。

- **怎么填**：先在地面选地砖规格 / 铺贴方向 / 缝宽；再按户型图**一间间填房间**（每间填"离原点左/上 + 长 × 宽"），平面图实时画出来。
- **房间可以直接拖**：在全屋平面图上**按住房间拖**（手机上也行）。拖到隔壁房间的墙边会**自动吸附对齐**——贴墙（留出一个墙厚）、两边齐平、或者直接贴死；吸上时画一条蓝提示线告诉你跟哪间对齐了。离得远就按 10mm 网格走，不会跳。拖完坐标自动写回下面的数字框，想精确改还是改数字。墙厚在设置里填（默认 240）。
  - 拖到原点左边/上边之后，程序会**把全屋整体平移**回 0 起，免得出现负数坐标。
- **起铺基准**：指定一间"参考房"（一般选客厅），程序按"居中对称 + 边缘裁砖 ≥ 1/3 砖"定下**全屋唯一的格线原点**；底下还有「整网右移 / 下移」两个框可以自己微调。
- **通铺怎么保证**：全屋只算**一个相位**，每个房间只是把这张网切出自己那一块——所以缝在门口天然连成一条线。平面图上砖缝按"至少 1 像素"画出来（全屋图缩得小，不补画的话看不见缝）。
- **门口每道可选**：「通铺（缝穿过去）」或「过门石（这里断开）」——过门石那一段不铺砖，另给过门石尺寸。
- **产出**：全屋平面图（房间名、门洞、每间房要不要标裁砖尺寸可开关）、**每间房的裁砖清单**、以及**下料清单**（整砖几块、每种裁砖几块、按 5% 损耗的备料块数、铺贴面积）。

**两句实话**：
- 门洞比砖窄时，穿过门口那块砖两侧要**开缺口**避开墙，形状不是矩形——程序如实提示「跨门口开缺口 ×N」，让现场照缺口切，不假装它是块矩形砖。
- 全屋平面图在手机上是缩略图，看不准的尺寸看"每间房明细"里的清单。

验收：`node house-test.js`（45 条，含"通铺"这条命门——相邻房间的砖缝必须落在同一张网上）。

## 十八、房间布局草图能画门洞了，导出时门跟着一起走（2026-09-16）

> 还是只动 `房间布局草图.html` 一个文件，`house.html` **一个字没改**。

**它解决什么**：以前草图里只能摆房间、设墙厚，**摆不了门**。而导出那一步写死了"门洞清空"，
所以每按一次「应用到 house.html」，`house.html` 里量好的门**全被清掉**，
只能回去一道道重量——量差几十毫米，排出来的砖就不对。

- **点墙就开门**：手指点两间房中间那道墙，门就开在点的位置（点得不太准也算，附近就认）。
  门开出来自动选中，下面「门洞」卡片里能改门宽、切成**通铺**（砖缝穿过去）还是**过门石**（门口断开）。
- **门能顺着墙拖**：按住门左右拖，拖到墙头会自己停住。拖动时不会跟同一道墙上已有的门叠在一起。
- **窗口对不上就自动夹回来**：把房间拖来拖去，门跟着墙走；夹不住的会自动贴着墙边站好，
  **不会动不动就变红**。只有真放不下的时候（房间被拖得不再相邻、中间的缝被第三间房占了、
  共用段比门还窄）才画红叉、清单里报红，并且**导出时逐条告诉你为什么丢**。
- **导出带门过去**：确认框会写清楚"`house.html` 现有几间房几道门 → 换成草图这几间几道门"，
  草图里一道门都没画时会**明确说"全部清掉"**，不会让人以为门还在。
- **两边显示的数字一模一样**：草图清单里的「离起点 1500」和 `house.html` 里的 `at` 是同一个数，
  机器测过逐字相等。**建议门在草图画好，别回 `house.html` 改门的"从哪间 / 到哪间"**——
  对调之后同一道门会跳到墙的另一头。

**顺手修的两个老毛病**：

- **房间画错位置**：画布是竖的、户型是横的时候，房间会整体画高近 2 米（实测偏 1757mm），
  而点击判定用的是对的位置——**看得见的和点得到的对不上**。已修，现在误差 0.9 像素以内，
  并且加了断言锁住（故意把 bug 放回去验过，会红）。
- **门宽数字看不清**：竖墙上的门宽数字跟门洞线同色、又压在虚线上，虚线的洞从笔画缝里透出来，
  远看像一串珠子。现在数字先垫一层底色再填，明暗两套主题都清楚。

**两句实话（已知限制）**：

- **同一道墙上两道门叠在一起**，`house.html` **不报错但会把数字算错**（那块砖的面积被算了两次，
  全屋面积和裁砖清单都会偏）。草图这边**已经挡住了**：拖门拖不到别的门身上，在已开门的位置再点
  只会选中那道门。但 `house.html` 自己手工加门仍可叠——那是它的老毛病，这次没动它。
- **起铺基准房**（`house.html` 的"参考房"）导出时是按房间顺序重新编号的。如果草图的房间顺序
  变了，这个基准可能悄悄指向另一间房，确认框会提示"会退回用第一间房"。

验收：`node sketch-test.js`（51 条，含跨工具契约——草图造 300 组随机户型的门，
喂进 `house.html` 的真引擎必须全部合法；以及 2000 组镜相对照，两边算法**逐字段相等**）。

---

## 十九、房间布局草图：加了「撤销」按钮（2026-09-17）

> 还是只动 `房间布局草图.html` 一个文件，`house.html` **一个字没改**。

**它解决什么**：以前房间是手指按住就能拖的，**拖错了没有任何回退办法**。手一抖把客厅拖到主卧身上、
或者把一间房拖出屏幕，只能靠"拖回去"——而拖动是吸附到 100mm 网格的，拖回去根本对不上原来的数
（原本坐标是 0，拖歪了再拖回来可能就是 100）。删房间、删门、清空虽然有确认框，但**确认了就没得救**。

现在工具栏多一个 **「↶ 撤销」**，就在「清空」旁边——清空是破坏性的、永远排最后，撤销贴着它，
手滑点完清空之后手指正好在旁边。

- **一次撤销 = 一次动作**，不是一次数据改动。拖一次房间撤一次、改一次门宽撤一次，
  不会出现"拖一下要按好几下才回去"。
- **能撤销哪些**：拖房间 · 拖门 · 点墙开门 · 删房间 · 删门 · 清空 · 改房间名字和长宽 ·
  改门宽和位置 · 通铺↔过门石 · 贴死↔留墙厚 · 墙厚预设 · 新门的默认宽度和类型。**基本上能改的都能撤。**
- **没改动就不占位置**：「按了房间但没拖动」「点墙结果撞上已有的门、没真开出来」这种
  不会记一笔——按了撤销却什么都没发生，比不能撤销更让人迷惑。
- **最多记 20 步**，超了就把最老的丢掉。
- **台式机上还能按 Ctrl+Z（Mac 是 ⌘+Z）**。鼠标焦点在输入框里时不接管，不会抢掉输入框自己的撤销。

**三条要知道的限制**：

- **刷新页面就没了**。撤销记录只存在内存里，关掉或刷新就清空，按钮变灰。
  想撤回上一次改动，得在刷新之前按。
- **撤销不会连缩放平移一起还原**。缩放和拖动画面是"看图的动作"、不是"改动"，
  顺手还原会很烦。**但撤销完如果房间跑到屏幕外了**（比如撤销"清空"，房间是原地复活、
  而当时的视野是按空户型算的），画面会自己重新适应一次，把房间找回来。
- **撤销只认这个工具里的改动**。在 `house.html` 里改的东西，草图这边撤销不了。

**顺手修的两个老毛病**：

- **"点了一下墙，门没开出来，却占掉一格撤销"**：门编号以前是先自增、再判断能不能开；
  撞上已有的门或者位置放不下时，编号已经白涨了一格。而编号存在撤销记录里，
  于是"什么都没开出来"也会记一笔。已改成**真建成了再自增**。
- **改完数字点一下画布，那次修改撤销不了**：输入框的"打完了"判定原来靠失焦，
  而"打完字去点画布"这个最常见的动作，事件顺序上会让这段编辑**被悄悄丢掉**。
  现在改用"停手 0.7 秒算打完"，并且**按下画布时先把上一步结算掉、绝不直接扔**。

验收：`node sketch-test.js`（51 条）和 `node house-test.js`（51 条）一条不少全过
（这次没碰排版引擎、也没碰导出契约）；另外扩了无头浏览器的真手势测试，补 18 条撤销断言，
**明暗两套主题各跑一遍**：拖房间→撤销坐标逐字回到原值、点墙开门→撤销门数门位一起复原、
**清空→撤销房间和门全部回来**（这条最值钱）、改数字后马上点画布→撤销能回原值、
一路撤到底按钮变灰且空栈硬按不报错、撤销后刷新页面数据仍是撤销后的样子。

---

## 二十、房间布局草图：每间房都显示面积（2026-09-17）

以前只知道房间是「3600×3000」，多大得自己乘。现在**每间房直接写着几平米**。

### 显示在两处

| 位置 | 样子 |
|---|---|
| 画布上的房间 | 第三行，接在名字和尺寸下面：`客厅` / `3600×3000` / `10.80 m²` |
| 下面的房间清单 | 那行末尾：`客厅　x 0　y 0　3600×3000　10.80 m²` |

两处用的是**同一个算式**，不会出现"上面一个数、下面另一个数"。

### 数字格式

**完全照抄 `house.html` 的「铺贴面积」**：保留 2 位小数 + `m²`（不是「㎡」这个字）。
两个工具对同一间房报出来的写法一样，并排看不会犯嘀咕。

### 一件要提前说清楚的事

**把每间房的面积加起来，不等于「全屋通铺」算出来的「铺贴面积」**。差在两处：

- 通铺那边算的是**含门口过道**的整片铺贴区域，而且**重叠的部分只算一次**
  （两间房共用的墙缝、门口那一小条，不会重复加）；
- 这边是**一间一间单独算**的，房间有多大就是多大。

所以两个数对不上是正常的，不是哪边算错了。想知道"一共要用多少砖"，看 `house.html`
的「铺贴面积」；想知道"这间房多大"，看这里的每间房面积。

### 一个刻意的取舍

**画布上房间太小时，面积那行不显示**（尺寸那行也一样）。门洞标签、房间名字已经占着地方，
硬塞进去会叠在别的房间上，比不显示更难看。**把画面放大，房间够大它就出来了**——
下面的房间清单里则是**每间永远都有**，不受大小影响。

### 验收

- `node sketch-test.js`（51 条）、`node house-test.js`（51 条）一条不少全过
  ——这次只动界面、没碰排版引擎和导出契约，全过才是正常的。
- 无头浏览器补 6 条面积断言，**明暗两套主题各跑一遍**：清单里三间房分别是
  `10.80 m²` / `9.00 m²` / `8.64 m²`，且整行形状是
  「名字 x N y N W×H A.AA m²」——把 2 位小数和 `m²` 这个字都锁住了，
  谁哪天改成 1 位小数、或者换成「㎡」，测试立刻变红。

---

## 二十一、全屋通铺：平面图卡片上多了「标注尺寸」按钮（2026-09-17）

以前想看图上的小字标注，得**把「设置」面板展开、一直滚到底**，才看到那个开关。
功能一直都在，就是藏得太深——**要翻半天才找得到的东西，等于没有**。

现在「全屋平面图」卡片上，标题底下就有一个按钮：**点一下就标出来，再点一下收起来**。
开的时候它是蓝底蓝字、亮着的，一眼能看出当前是哪种状态。

### 这个按钮管两样东西

一开始它只管裁砖尺寸，后来发现**门口那行小字也该归它管**，就一起收进来了：

1. **裁砖尺寸线**：每一条裁砖的边画出红色尺寸线，标着**实际裁多少毫米**
   （比如「749」= 这条边上那块砖要裁成 749mm）。拿这个数去跟瓦工对，或者估料。
2. **门洞标签**：门口那行「过门石 900」/「通铺 900」。

两样都是**画在图上、压着砖缝的多余小字**，所以一起开、一起关才说得通——
只收一半，剩下那一半照样挤。按钮名字也因此从「标注裁砖尺寸」改成了**「标注尺寸」**。

### 关掉之后，门口的信息一点没少

门洞标签收起来了，但**门洞本身照画不误**，而且照样一眼能分出是哪种：

- **过门石** = 红色实线框；**通铺** = 蓝色虚线框（见卡片下面那行图例）。
- **洞口多宽也没丢**：下面「门洞」卡片里，每道门都写着「过门石（这里断开），洞宽 900」。

所以关掉的只是**图上那行字**，不是这个信息本身。

### 两个开关，一个状态

设置面板最底下那个开关**原样保留**了，没有删。两个控件读写的是**同一个开关**，
所以**在哪边点都一样**，另一边会立刻跟着变——不会出现"按钮亮着、开关却是关的"。

（这么写是有意的：按钮和开关各刷各的，是这类"一个功能两个入口"最容易出的 bug。
代码里两个控件都不直接改数据，一律走同一个 `setShowCuts()`，由它把两边一起刷新。）

### 顺带照顾了读屏软件

新按钮带 `aria-pressed`，看不见蓝色高亮的用户，用读屏软件也能听出当前是开还是关。

### 什么时候用得上

- **平时关着**：字太多，密密麻麻压在房间上，反而看不清砖缝对不对得齐。
- **要对细节时打开**：这时候才需要那些毫米数。

### 验收

- `node house-test.js`（51 条）、`node sketch-test.js`（51 条）全过
  ——这次只动界面、没碰排版引擎，全过才是正常的。
- 无头浏览器补 21 条断言，**明暗两套主题各跑一遍**，重点锁四件事：
  1. **按钮 → 开关**：点按钮，设置里的开关跟着打开；
  2. **开关 → 按钮**：从设置里关掉，按钮的高亮跟着灭，**平面图逐字回到最初那张**
     （锁的是"没有残留"，不是"看起来差不多"）；
  3. **刷新还在**：关掉浏览器再开，状态记得住（锁的是那句 `save()`）；
  4. **该收的收干净、不该收的别动**：关着的时候，画布上**一个字都不该写**
     「过门石 …」或裸的毫米数（拦的是 `fillText`，直接看它画了哪些字符串，
     不靠数像素猜），但**房间名（客厅/主卧）必须还在**。
  另外数了画布上的红像素：关着 300 多（那就是门洞的红框），打开涨到 5000 左右
  ——证明裁砖标注**真的画出来了**，不是只把按钮点亮了。

---

## 二十二、全屋通铺：门口放了过门石，两边各自重新排砖（2026-09-18）

> **⚠️ 这一节里的「分区规则 第 1 条」后来被推翻了**，请连着下一节（二十三）一起看。
> 当时定的第 1 条是「**只有过门石才断开**，有墙没放过门石就照旧通缝」——**这条是错的**，
> 下一节改掉了。本节其余内容（过门石断开、各区各自起铺、通铺优先、贴死提醒）仍然有效。

### 原来哪里别扭

这个工具从第一天起的前提就是「**全屋只有一张砖网**」，砖缝的对齐全靠一个全局原点
推出来——这正是「通铺」的由来。

可门口标了「过门石」的时候，工具只做了一半：那块门口确实不铺砖了，**但两边房间的
砖缝还是被拽着必须对齐**。

现场放一块过门石，本来就是为了**盖住那道缝**。结果工具反过来逼着两边通缝，
过门石等于白放；而且两个区都被迫跟着同一张网走，自己那边的边缘碎砖反而更难看。

### 改完之后

过门石成了真正的「隔断」：放了过门石的地方，两边**各按各自最好的方式起铺**，
缝不用对上；没放过门石的地方，全屋照旧一张网。

**分区规则**（两条都是当时一起定下的）：

1. **只有过门石才断开。** 两间房之间有墙、没放过门石 → 照旧通缝，跟以前一模一样。
   **一道过门石都没有的户型，排出来的结果和改之前逐位相同**（下面有回归锁守着）。
2. **以哪间房为准起铺**：设置里选的那间，管**它自己所在的那个区**；
   其余每个区，以**该区里面积最大的那间房**为准（一样大就取房间清单里靠前的）。

顺带定了一条边界：**同一道墙上既有「通铺」又有「过门石」时，通铺赢，不断开**
——因为「通铺」的意思就是砖从这儿铺过去，缝本来就要对上。

### 界面上能看到的

- **平面图**：过门石＝**红实线**框（这里断开），通铺＝**蓝虚线**框（缝穿过去）。
- **平面图标题下那行**：分区时写「2 个区，分别以「客厅」「主卧」为准起铺」；
  只有一个区时，还是原来那句「以「客厅」为准起铺」，一个字没改。
- **下面那条提示**：分区时点名**哪几间房属于同一个区**，并说明「过门石两侧各排各的，
  不用对上」。
- **设置面板**「以哪间房为准起铺」下面多一行小字，**只在真的分成多个区时才出现**。
- **新增一条提醒**：过门石两侧是**贴死**的（中间没有墙厚）时给出提示——
  过门石本身也就一道缝那么宽，两边缝又对不上，会直接露出来，建议留墙厚或改成通铺。

> 还会遇到一种情况：放了过门石，两边却没能分开。那是因为这两间房**还从别的门口连着**
> （绕一圈还能走过去）。连通分量本来就是这样，工具会如实说明，不会假装分开了。

### 验收

- `node house-test.js`：**51 条 → 100 条，全过**（下一节又加到 126 条）。其中一条是**回归锁**——没有过门石时
  把今天跑出来的真实数字（格线原点 497 / 598、整砖 27 块、裁砖 48 块、铺贴面积
  35653992 mm²）写死；将来谁顺手改坏了，测试立刻红灯。
- `node sketch-test.js`（51 条）、`node test.js`（卫生间工具，317 条）全过
  ——这次没碰这两个文件，全过才说明没误伤。
- **无头浏览器**：明暗两套主题 ×（2 个区的户型 / 1 个区的对照），逐条核了分区数、
  门口用的网、三处文案、那行小字该不该出现、横向不溢出、JS 零报错。
- **门口那一段实测**：门洞横向 4690–5590mm。按**本区的网**，砖缝落在 **5188mm**；
  按老办法（拿客厅那张网画）落在 **5458mm**——**差了 270mm**。
  九百宽的门洞里差 270，肉眼一眼就看出来接不上，所以这处必须按各自的网画。

### 一处需要留意（不是 bug）

加了过门石之后，**两侧的砖块总数会变**——原来跟着全屋那张网算的砖，现在要按自己
那个区重新算。这正是「重新排砖」的意思。

另外提醒一句：因为规则是「每个区以区内最大的房间为准」，**以后往某个区里加一间
很大的房间，那个区的起铺基准会跟着换**，整区的砖缝位置会重排。这是当初选的方案，
知道有这回事就行。

---

## 二十三、全屋通铺：墙就是隔断——墙上没开门洞，两边就各排各的（2026-09-18）

### 原来哪里别扭

上一节（二十二）把规则定成了「**只有过门石才断开**」：两间房之间只要**有墙**，
不管墙上开没开门洞，工具都算它们「通着」，共用一张网。**这个前提是错的。**

欢欢的原话：

> 墙本身就是把房间隔开了，只是从门口处相连，门口选择通缝缝就穿过去，
> 如果选择过门石，房间就被打断成独立的了

错在哪，拿主卧举例最清楚：主卧门口放了过门石，但它和次卧之间那道墙**根本没开门**
——工具却当这两间通着，于是又把主卧拽回全屋那张网，**过门石白放**。
而且原来那条提醒只说「还从别的门口连着」，用户会满屋子去找一个根本不存在的门口。

### 改完之后

**墙就是隔断，门洞才是通道。** 两间房之间算不算「通着」，看这张表：

| 两间房之间 | 结果 |
|---|---|
| **贴死**（中间没有墙厚） | 通着 —— 本来就是一个空间（比如客餐厅一体） |
| 有墙 + 开了「**通铺**」门洞 | 通着 —— 缝从门口穿过去 |
| 有墙 + 开了「**过门石**」门洞 | **断开** —— 两边各排各的 |
| 有墙 + **没开门洞** | **断开** ← 这次改的就是这一条 |

三条老规矩照旧：**通铺优先**（同一道墙上既有通铺又有过门石 → 不断开，缝穿过去）；
**断开只切直接那一道边**（两间房还从别的路通着的话，绕一圈照样是一张网）；
**同一个区里的砖缝仍然完全连通**。

### 不放过门石的户型，一个像素都没变

**一块过门石都不放 → 全屋还是一张网，跟改之前逐位相同。** 这是界面上写明的承诺
（「没有门洞也能用——全屋一张网铺过去」），代码里那条捷径原样留着，
测试里也有回归锁守着（格线原点 497 / 598、整砖 27 块、裁砖 48 块、面积 35653992 mm²）。

要留意一个**副作用**（是有意的，不是 bug）：放上**第一块**过门石的那一刻，
那些「有墙、又还没画门洞」的房间会跟着一起独立出去。这是「墙就是隔断」的必然结果，
工具会提醒，按提醒补上门洞即可。

### 界面上能看到的

- **图例**加了「**墙就是隔断**：要连通就在这道墙上开个门洞」。
- 「门洞」卡片下面多一句：墙上没开门洞的地方也当隔断，两边各排各的。
- **分区状态条**不再说「（中间有 N 道过门石）」——墙也会分区，这个等式不成立了；
  改成只报「分成 N 个区」以及每个区里有哪几间房。
- **设置面板**那行小字：「**墙和过门石**把全屋分成了 N 个区」。
- 提醒里会**点名绕经哪几间房**（「还绕经「次卧」连着」），不再含糊地说「还从别的门口连着」。
- **新增一条提醒**：某间房四周的墙上一道门洞都没有 → 它现在自己单独成一个区，
  提醒回去补一道门洞；如果本来就是这么设计的，忽略即可。
  判据是「**一道门洞都没有**」，**不是**「自己一个区」——主卧只靠一块过门石进出时
  也是自己一个区，那是用户自己选的，不报警。

### 验收

- `node house-test.js`：**100 条 → 126 条，全过**。新增的回归锁里有一条就是欢欢报的那个
  户型：走廊→主卧放的是过门石，主卧和次卧共用一道**没开门洞**的墙 → 主卧必须独立。
  主卧那个区的格线原点写死成 **1888**——改之前是 **448**，也就是全屋那张网的值，
  **这个数字本身就是「过门石白放」的证据**。
- 反向也锁了：在那道墙上补一个「通铺」门洞 → 主卧**回到**全屋那张网。
- 另外锁了：贴死的两间房照旧通着；有墙 + 通铺门即便别处有别的过门石，也仍是一张网
  （证明走的不是那条捷径，不是白测）。
- `node sketch-test.js`（51 条）、`node test.js`（卫生间工具，317 条）全过
  ——这次没碰这两个文件，全过才说明没误伤。
- **无头浏览器**：仿那个户型跑了一遍，明暗两套主题各截了图。主卧的砖缝落点，
  按「它自己那个区的网」是 **1440 / 1888 / 2690 / 3492 / 4294 mm**，
  按全屋那张网是 **1440 / 2101 / 2903 / 3705 / 4507 mm**——明显不同；
  补上那个通铺门洞之后，两者重合。JS 全程零报错。

### 一处已知边界（这次没动）

`wallBetween` 在两间房离得很远、但横向或纵向有重叠时，也会算出一道「墙」
（墙厚是个几百上千毫米的怪数）。这是老毛病，这次没碰。
好在按新规则，这种「假墙」不会再自作主张把两个区并起来；
只有用户**真的**在这种位置画了一个「通铺」门洞，才会并区。

---

## 二十四、房间布局草图：量完房一口气把尺寸贴进来（2026-09-18）

### 原来哪里别扭

画草图得**一间一间加**：点「＋ 添加房间」→ 填名字 → 填长 → 填宽 → 再点一次。
量完一整套房，手里攥着七八个数字，却要在手机上戳七八轮。真正费事的不是画图，
是**把已经量好的数字输进去**。

### 现在怎么用

草图页多了一张「**批量贴尺寸**」卡片。一行一间房，**先长后宽**，单位毫米：

```
客厅 4200x3600
主卧 3300x3000
次卧 3300x3000
```

粘进去 → 点「加进草图」→ 房间全出来了，再拖着摆位置。

- **名字可以省。** 只写 `3000x3000` 就自动叫「房间1」「房间2」；
  已经有过「房间3」就从 **4** 接着编，不会撞名（按名字查，不是按数量——
  否则删掉中间一间再加，会冒出两个「房间2」）。
- **分隔符不挑。** `x`、`×`、`*`、`X`、逗号、空格、横杠都认；
  `4200mm x 3600mm`、「客厅4200x3600」、「主卧：3300×3000」也认。
- **「长 / 宽」两个字可以带上**：`书房 长2700 宽3300`。
- **空行、`#` 和 `//` 开头的注释行自动跳过。**
- 新房间先摆在**现有户型的右边**，四间一行往下摞；加完自动重算视野，
  不会飘到屏幕外。
- 加进来是**一步**：点一次「↶ 撤销」整批退回。

### 认不出来就报错，绝不猜

这是这张卡片最重要的一条：

> **宁可报错，也不替你猜一个数。**

因为猜错的代价不对称。报错，用户当场改一个数字；
猜错（比如把「卧室2」名字里的 **2** 当成长度），用户会带着**一间尺寸错的房**
一路导进 `house.html`、排完砖、拿到下料清单，等到贴的时候才发现。
所以：

- 一行认不出「长 × 宽」两个数 → **这一行不加**，红字点名第几行、原文长什么样，
  并把**输入框原文给你留着**，方便对着找是哪行写坏了（全认对了才清空输入框）。
- 其余认得出的行**照常加进去**——一行写坏不该连累整批。
- **尺寸小于 300mm 直接报错**，并提示「是不是把米当成毫米写了」。
  防的就是 `客厅 4.2x3.6`：真按毫米算会得到一间 **4×4 毫米**的房间，
  界面上看着像加成功了。
- 名字里带数字没问题：`卧室2 3000x3000` 认的是名字「卧室2」+ 3000×3000。
  正则锚在行尾就是为了这个——不锚会把 2 当成长度，尺寸整条错位。

### 怎么验的

- `node sketch-test.js`：**51 条 → 84 条，全过**。新增 33 条钉的就是上面这些：
  各种分隔符与写法、自动编号不撞名、空行注释不占行号不占编号、
  **报错行号要准**（用户是对着输入框数行号的）、报错绝不猜、小数取整。
- **无头浏览器跑了 24 条**（`/tmp/tp-shot/batchcheck.js`），验的是单测碰不到的
  DOM 那一段：粘贴 → 点按钮 → 房间真的出现在清单里、撤销整批退回、
  写坏一行时原文留在框里、点「清掉输入框」不动房间、刷新后还在、
  加完视野重算把新房圈进可见范围。**JS 全程零报错。**
- 还查出并修掉一个小毛病：输入框是**空的**时候点「加进草图」，
  提示原来也是红字——对着一个空框报红像出了事一样，改成了普通提示色。
  红色只留给「你真写错了」。
- `node house-test.js`（126 条）、`node test.js`（317 条）全过——这次没碰这两个文件。

### 下一步（还没做）

这解决的是「数字怎么进得来」。往上一步，是**数字怎么量出来**——
用蓝牙激光测距仪直接读数、不用手抄。那要等这一步先用一阵子，
看清到底值不值得再买仪器（Bosch GLM 50-27 CG 约 ¥959，协议是公开的）。

---

## 二十五、全屋通铺：瓷砖靠墙留 5mm 缝（2026-09-22）

欢欢的原话：

> 全屋通铺里面瓷砖的铺贴规则加一条：瓷砖与墙之间留5mm缝隙

### 原来哪里别扭

这个工具里，**最外圈那块砖是齐平贴到墙上的**。这不是漏写了，是几何模型的直接后果：
房间矩形就是墙的内表面，砖网按这个矩形裁剪，最后一块砖的边界正好落在墙面上。

真实施工不是这样。墙不一定直、砖不一定方正，靠墙那圈**必须留一道缝**：
给热胀冷缩留余量，也给墙面误差留余量，还得塞得下十字架、后期打得进胶收口。
图纸按齐平排，下料清单里那圈砖就**都大 5mm**，贴的时候现场还得自己再想一遍
——这正是这个工具该替用户想掉的事。

### 改完之后

**全屋每间房靠墙的那一圈砖，自动裁窄 5mm。** 缝宽在设置里能改，默认 5。

做法上有个刻意的选择：**砖网的起点一个毫米都不动，只把「铺到哪儿为止」往里收 5mm。**
这跟真实施工顺序是一个道理——在墙上弹好线、先排整砖网，靠墙那圈现场裁窄。
好处是**全屋的砖缝对位关系完全不变**：改之前能对上的缝，改之后照样对上。

四条边怎么处理：

| 这条边是 | 结果 |
|---|---|
| 墙（含外墙） | 缩 5mm |
| **两间房贴死**（中间没有墙厚，比如客餐厅一体） | **不缩**——那本来就是同一个空间 |
| 过门石两侧 | 各自缩自己那侧 5mm |
| 通铺门洞 | 洞口的砖**不受影响**，照旧是一整块穿过去 |

### 界面上能看到的

- 设置面板「砖缝」下面多一行「**靠墙缝（mm）**」，默认 5。
- 顶部标题那行摘要、图上图例的小字，都跟着显示当前值（`… · 缝 2mm · 靠墙 5mm`）。
- 图例下面补了一句：靠墙那一圈砖会自动裁窄（墙不一定直、砖不一定方，靠墙得留一道缝），
  在设置里改；两间房贴死（中间没墙）的那条边不留。
- **「要裁的砖」清单里的数字会变小**——这是最实在的一条。同一间房同一块砖，
  改之前写 `646×800`，改之后写 `641×800`，**正好小 5mm**。照着下料不会多切。

### 验收

- `node house-test.js`：**126 条 → 155 条，全过**。
- `node sketch-test.js`（84 条）、`node test.js`（卫生间，317 条）全过
  ——这两个文件这次一行没碰，全过才说明没误伤。
- **回归锁：填 0 时逐位等于改之前**（格线原点 497 / 598、整砖 27 块、裁砖 48 块、
  面积 35653992 mm²）。老存档（压根没这个字段）和显式填 0 两条路径，结果逐位相同。
  这是「你不填，就当没这回事」的保证。
- 两条新锁专门守着上面那张表：**贴死的两间房不许在中间凭空凿出一条缝**
  （整砖 21 / 裁砖 26，和改之前一模一样）；**跨门洞那块砖仍是一整块**
  （不冒出「异形砖」的假警报）。
- **无头浏览器跑了 51 条**（`/tmp/tp-shot/gapcheck.js`）＋明暗两套主题截图，
  JS 全程零报错。缝宽是**用像素量的**：把单间小屋放大到 1mm ≈ 1.21 个设备像素，
  量出来 `0mm → 2.81`、`5mm → 7.81`、`20mm → 25.81`、`40mm → 49.81` 像素，
  20 到 40 之间的斜率 **1.20**（真值 1.21）——**这条直线就证明缝是按毫米算出来的，
  不是画上去的**。再把改缝前后的两张图逐像素相减：变动的像素**全部贴着房间四边**，
  离墙最远 0.27 个像素，**屋子中间一个像素都没动**。

### 验收时抓出来的两个毛病（都已修掉）

**一、我自己刚写出来的 bug：整个页面白屏。**
页面那侧要用「老存档兜底」那个小函数，它是引擎内部的，外面看不见；
我没取出来就直接调，页面一加载就报错，图全没了。
**三个 node 测试在结构上抓不到这种错**——它们只把引擎那一段抠出来跑，永远碰不到页面代码。
这就是为什么每改一次都要真开一次浏览器，不能只看单测变绿。

**二、1/3 裁砖警告里的数字会骗人。**
「边缘裁砖最窄 XXXmm」这条警告，判据改成了看**名义宽**（把靠墙缝加回去再比），
这是对的：真拿裁完的净宽去判，会有约 2.2% 本来合格的边条凭空掉进警告里
（800 的砖、名义 271 够，裁完 266 差 0.67mm 就踩线），纯属打扰。
但**印出来的数字跟着一起用了名义宽**，就出问题了——警告写着「最窄 **114**mm」，
用户翻到裁砖清单看到的是 **109**mm，整整差一个缝宽。
现在判据照旧看名义宽（不多报警），**印出来的数字换回真砖宽**（和清单对得上）。
两条都配了回归锁。

### 一处已知边界

**同一条边一半是墙、一半贴死**（比如两间房共边，一段挨着贴死的邻居、一段是墙），
按**贴死优先**处理：整条边都不缩。代价是那段真墙边上少 5mm 缝（面积误差通常不到 0.1%）。
反过来「墙优先」会在贴死的那一段凿出 5mm 的槽，又变回「凭空多一条缝」那个毛病。
精确解要把房间切成小块，改动大一个量级，不值得，先记在这儿。

### 怎么关掉

**在「靠墙缝（mm）」里填 0**，全屋立刻回到齐平贴墙，跟这个功能没加过一样。
把输入框**清空**是回到默认的 5mm（不是 0）——清空的意思是「我没意见，按默认来」，
想明确不留缝就填 0。填得再大也会卡在 50mm 以内，防手滑填成 5000。

---

## 二十六、全屋通铺：从某个墙角起铺（起铺角）（2026-09-23）

欢欢的原话：

> 起铺点至基准房的那个角，显示默认自动选一个，旁边放一个"换一个"按钮，总计就2版
>
> 第一，那个角可以是客厅的四个角中的一个，需要用户自己选；第二，起铺点是需要让出墙缝的，
> 不管是从起铺角排砖还是居中排砖；第三，小于10mm的窄条可以忽略不计
>
> 乙，各自决定

### 原来哪里别扭

之前只有「**居中对称**」和「**靠一边**」两种排法。居中对称是**从房间正中间往两边排**，
好看、对缝整齐；但它有个毛病——**四面墙那一圈全是裁砖**，每块都要现场切一刀。

真实施工里，师傅会尽量避开裁砖：**要是从墙角起铺，那两面墙贴的都是整砖，一刀都不用切。**
这是很省事的一招，图纸上却排不出来。

### 改完之后

设置面板多了一行「**起铺方式**」，三选一：`起铺角` / `居中对称`（默认）/ `靠一边`。
选「起铺角」之后，下面出现第二行「起铺角（**基准房**的角）」，四个按钮 `左上 / 右上 / 左下 / 右下`——
默认替你选好左上，旁边还有个小按钮「**换一个**」。

**「换一个」就是切回居中对称那一版。** 欢欢说「总计就2版」，所以按钮只在**这两版之间**来回切，
不另开第三个开关——多一个开关就变成三版了。

**角上那块砖是整砖 800，一个毫米都不裁。** 靠墙那 5mm 缝，留在**砖和墙之间**：

```
墙 ║←5mm 缝→│←—————— 整砖 800 ——————→│← 缝 →│ …
             ↑
        在这里弹一条线，砖网从这条线起排
```

这是欢欢拍板的「**读法乙**」。另一种读法（甲）是让砖从墙面起排、把角上那块裁成 795——
那样角上照样是块裁砖，起铺角就白起了，所以没采用。

**两个方向各自决定**（欢欢说的「各自决定」）：左右能起铺角就起，上下该退回居中就退，
互不牵连。所以「左右整砖、上下居中」这种**半角状态**是正常的，不是半成品。

**什么时候会退回居中**——对面墙那块砖太难看的时候：

| 对面墙剩下的零头 | 怎么办 |
|---|---|
| 小于 **10mm** | **忽略不计**（欢欢的原话），照样用起铺角——这么窄的条根本看不见 |
| 10mm 到 1/3 砖之间 | **这个方向退回居中**——一条又窄又显眼的零头，比四周均匀裁砖更难看 |
| 1/3 砖以上 | 留起铺角，那一条现场裁一刀，值 |

跟「二十五」节一样：**砖网的起点一个毫米都不动，只是换个起点。**
所以改回居中时，结果和功能没加过**逐位相同**（有回归锁盯着）。

### 界面上能看到的

- 选「起铺角」时，那行小字会**照实说**结果，三种话不合并：
  - **成立**：`以「客厅」的左上角起铺：左墙贴的是整砖，对面右墙剩 795mm 现场裁。`
  - **退回居中**：`……，上下这一向改回居中对称（对面墙只剩 93mm）。`——**为什么退**写在括号里
  - **一个角都没用上**：`这个角用不上——左右、上下两个方向的对面墙都只剩太窄的零头，已自动改回居中对称。换个角试试。`
    （这时候前面那句「以某角起铺」是假话，引擎会把它整句换掉，不硬说）
- 顶部摘要行只在**不是默认**时才多一个词：`… · 缝 2mm · 靠墙 5mm · 起铺角`。
  默认那版（居中对称）不提，说了等于白说，还把那一行挤长。
- 角的位置（左上/右下）**也不写进摘要行**——紧跟着的小字说得比这清楚。

### 验收

- `node house-test.js`：**155 条 → 179 条，全过**。
- `node sketch-test.js`（84 条）、`node test.js`（317 条）全过——这两个文件这次一行没碰。
- **回归锁**：不传新参数、或模式不是 `corner` 时，结果**逐位不变**（老路径一个字节没动）；
  **老存档压根没有「哪个角」这个字段时，钉死左上角**，绝不静默走错版。
- 其余 6 条新锁：角上那块**是整砖 800 而不是被切过的 795**（四个角全扫）；
  **<10mm 忽略、10~1/3 退回居中**（扫了一遍尺寸，三种状态都出现过）；
  **两轴各自决定**，且**退回居中的那根轴和真·居中模式逐位相同**；
  **说好忽略的零头不许再报一次警告**（10mm 以下闭嘴）；
  **整网偏移之后不许再声称角上贴的是整砖**。
- **无头浏览器跑了 46 条**（`/tmp/tp-shot/cornercheck.js`）＋明暗两套主题、
  7 张截图，JS 全程零报错。
- **读法乙是拿像素量出来的**，不是看代码看出来的。把 1619×900 的房按 1mm ≈ 0.73 个设备像素画出来，
  横着扫一整行：

  | 量什么 | 实测 | 说明 |
  |---|---|---|
  | 角砖的左沿离墙多少 | **4.9mm** | 读法甲（贴墙起排）会量出 **0**，一眼分得开 |
  | 角砖自己多宽 | **799.81mm** | 就是 800（795.71 + 4.1 的缝线宽）；读数法甲会量出 795 |
  | 同屋改回居中后，角上那块 | **403.32mm** | 对照项：排除了「它本来就是 800」这种巧合 |

  最后一行是这条验收的关键——**只有拿同一间房的居中版做对照**，
  才证明那个 800 是「起铺角」算出来的，而不是碰巧。

### 验收时抓出来的一个毛病（已修掉）

**测试脚本自己骗自己：数据和断言打架。**
有条断言打印 ❌，可它自己附的详情明明写着 `{"at":"corner","far":5,...}`——完全正确。
原因是我那个比较小数的工具函数 `near(a, b, tol)` **漏传了第三个参数**，
`Math.abs(0) <= undefined` 恒为假，于是断言拿着一份**正确的数据**报错。
补上默认值 `tol = 1e-6` 就好了。

这事值得记一笔：**这种"假红"比真 bug 还费时间**——你会先去怀疑引擎，
查半天才发现是量尺子本身歪了。所以断言打印的详情必须够全，
不然连"到底是数据错还是断言错"都分不出来。

### 一处已知边界

**填了「整网偏移」，起铺角就不作数了。** 偏移是在相位定完之后加到整张网上的，
会把起铺角那面墙的整砖一起带走。引擎**不会**悄悄改相位（师傅要整体挪，那就以师傅为准），
但小字会**明说**：`……整网挪过之后，左右这一向的起铺角就不作数了（整网右移 100mm）——画面上看到的格线是挪过的。想用起铺角，把「整网偏移」清成 0。`
宁可说「不作数」，也不能照着一份没挪过的算稿跟你说「左墙贴的是整砖」——那屏幕上明明错开一截。

另外两条跟「二十五」节同源：**同一条边一半是墙、一半贴死**时按贴死优先，整条边不缩，
起铺角那侧的 5mm 让位也就跟着没有（精确解要把房间切成小块，成本差一个量级，不做）；
**房间比一块砖还窄**时，角上那块自己就是裁砖，「起铺角」名不副实，直接退回居中。

### 怎么关掉

**「起铺方式」选回「居中对称」**，或点小字旁边的「**换一个**」，立刻就回到老样子。
老存档（没有这个设置）打开就是居中对称，跟没加过一样。

---

## 二十七、三个小修正：版本号、控件搬家、尺寸标注（2026-09-23）

欢欢的原话：

> 几个小问题：1，手机上只看得到更新的时间，看不到版本号，你把版本号加在更新时间后面；
> 2，你提到的新加的这几个功能要在设置面板里面拖到最下面才能看得见，我需要你把它放在全屋平面图旁边；
> 3，标注尺寸的时候有时看不清标的是哪一部分的尺寸

### 1. 版本号：挂在卡片上，不挂在页头

**原来哪里别扭**：`home.html` 页头只有一行「更新：<时间>」。手机上只看得见"什么时候更新的"，
看不见"现在是第几版"——而**判断手机有没有装上新版，恰恰就得看这个号**（`?v=` 加一没有）。

**改完之后**：三张工具卡片各自右上角一个版本号徽章 —— `v7` / `v15` / `v11`，
和链接里的 `?v=N` **一一对应**。页头那句话改成
「更新：2026-09-23 07:39 · 每个工具的版本号在它自己卡片上」，指个路。

**为什么没"加在更新时间后面"**：页头只有一个数字的话，说不清它是哪个工具的。
三个工具各走各的 `?v=`，改了一个不该动另外两个——一个数字一定会骗人。

> ⚠️ 以后改完某个工具：**卡片上的 `vN` 和链接里的 `?v=N` 必须一起 +1**，
> 只改一个等于白标（手机上看到的号跟实际加载的文件对不上）。

### 2. 靠墙缝 / 起铺方式 / 起铺角：从设置面板搬到平面图上

**原来哪里别扭**：这三样排在设置面板**最底下**。而设置面板是吸顶（sticky）的，
手机上（390×844）一展开比屏幕还高——想改一下靠墙缝，得先把它收起来、滚到平面图卡片、
再展开、再滚到底。改个数字要来回滚三趟。

**改完之后**：三样挪进「**全屋平面图**」卡片，就在画布正上方，和「标注尺寸」按钮同一行。

**一行 JS 都没动。** `renderSettings()` 和 `syncCorner()` 全都是
`document.getElementById(...)` 拿元素、事件也全按 id 绑——**只是标记搬了个家，逻辑原地不动**。
设置面板里还留着「以哪间房为准起铺」和「整网偏移」，那两样本来就在面板上半截，不碍事。

### 3. 标注尺寸：这是真 bug，不是"排得不好看"

**根因**：`paintCuts` 判断"这块裁砖贴着哪条边"用的是 `xs.start < 1`，
这句是**砖网从 0 铺起**的年代写的。第二十五节上了靠墙缝之后，铺贴区间从 **5mm** 起
（引擎的 `sideGaps`），`xs.start` 恒等于 **5** —— **这个条件永远为假**。

**后果**：左右两条宽标全掉到下边、上下两条高标全掉到右边，两个数字叠死在同一个角。
默认户型里，次卧那个 `591` 被一路挤到**房间外面**，孤零零飘着，连尺寸线都看不见——
这就是欢欢说的"看不清标的是哪一部分"。

**改法**（三处）：

- **按段的下标判**，代替按 `start` 判：第一段贴低边、最后一段贴高边（`latticeSegments` 按 `start` 升序）。
  于是宽标走上/下边、高标走左/右边，**四个角各站一个，互不挨着**。
- **标签往房间里面让开「半个标签 + 5px」**：原来原地压在尺寸线上，标签会把两端那对短刻度
  一起盖住，图上只剩一个孤零零的数字。让开之后尺寸线和刻度都露出来。
- 顺带修了 `flipH`（两个标签撞车时翻到对侧那条边）：翻过去之后同样让开尺寸线，别又压回线上。

> **语义没变**：翻边只换"数字站在哪条边上"，量的还是同一段裁砖
> ——线的两端刻度始终是那块砖的两个边。

### 界面上能看到的

- 平面图卡片从上到下：`标注尺寸` / `靠墙缝 5 mm` 一行 → `起铺方式` 三个按钮 → 图 → 图例。
- 图例那句原来的「万一数字被挤开了，会有一条虚线牵回它量的地方」**是错的**（代码里没有虚线，
  标签是连尺寸线一起挪的），已改成照实说：
  **「红线搭在哪一段砖上，旁边的数字就是那一段的尺寸」**。

### 一处已知边界

**特别窄的裁砖，尺寸线本身就短得看不见。** 比如次卧那条 60mm 的边条，
按屏幕比例只有七八个像素长，两端刻度几乎并成一个点——数字就贴在它旁边，
但指望看清那条线是不现实的。这是尺寸标注的固有极限（再放大就要改画布的缩放逻辑），
不是这次的 bug。

### 验收

- 三个引擎测试套件全绿：`house-test.js` **179 通过 0 失败**、`sketch-test.js` **84 通过 0 失败**、
  `test.js` **317 通过 0 失败**。
- 无头浏览器（390×844 手机视口）实跑：控件确实都在「全屋平面图」卡片里；
  靠墙缝填 8 → 图例跟着变 8 → 清空 → 回到 5；起铺角四个按钮、提示文字、
  「换一个」全部正常；三张卡片的徽章与 `?v=` 一致且不出屏；**全程零 pageerror**。

### 没动的

`index.html`（卫生间）和 `房间布局草图.html` 这两个文件**一行没改**，
它们的 `?v=` 也就没跟着动。

---

## 二十八、修一个真 bug：草图里的门，导进全屋通铺位置会偏（2026-09-23）

欢欢的原话：

> 发现一个bug：房间布局草图里面的过门石位置导入到全屋通铺里面位置不对

### 原来哪里别扭

在「房间布局草图」里拖一道门到某段墙的偏下位置，点【应用到全屋通铺】，
到了全屋平面图上，这道门（过门石）**跑到墙的另一头去了**——偏多少不一定，
但只要偏了，就是整段墙那么远。

### 病根：两个工具各拿一把尺子量同一段墙

两个工具量的都是"毫米"，但**尺子的零点不一样**。

- 草图画布很大，房间摆在画布的哪个位置就是哪个位置。门记住的是
  **画布上的绝对位置**（比如"从上往下 3440 毫米"）。
- 导出的时候，为了让 `house.html` 拿到一份干净的户型，所有房间会被**整体挪到左上角**
  从零开始（挪掉的量叫 `minX`/`minY`）。房间挪了，**门却忘了跟着挪**。

于是算门的位置时，一边用"画布上的绝对位置 3440"，另一边用"挪过之后那段墙的范围"——
**两把尺子差了整整一个平移量**，门就整体偏了那么多。

这个错只有在**户型没有贴着自己画布的左上角画**的时候才露出来。
而"把卧室往上拖一点"是完全正常的操作——
草图的拖动是**不限制**往左上拖的，一拖，`minY` 就成了负数，门就开始偏。

**最坑的是它不报错。** 偏了之后的门往往还是"合法"的（墙上别的位置确实放得下），
`house.html` 照单全收，只是默默画在错的地方。所以你看不到任何提示，只能靠自己发现位置不对。

### 改完之后

导出那道换算里，先把门的位置**减掉平移量**，再去和墙的范围比大小——两把尺子合成一把。

改动一共三处、十来行（`房间布局草图.html`）：

| 位置 | 改动 |
|---|---|
| `exportDoors` 函数签名 | 多收一个 `origin`（就是 `minX`/`minY`），并写清楚为什么不能省 |
| 夹取那一行 | `d.y/d.x` 先减掉 `origin`，再夹进共用段、再取整 |
| 调用处（`#apply`） | 把 `{ x: minX, y: minY }` 传进去 |

`origin` 是**可选**参数，不传就当 `(0,0)`——老户型（原点本来就在角上）走的还是原来那条路，
结果逐位不变。

### 为什么三个测试套件原来抓不到它

这是这次最值得记一笔的地方。`sketch-test.js` 里那条跨工具契约测试，**只验"门的合法性"，
从来不验"门的位置"**：

- 它构造的户型，原点偏移只有 `0` 或 `0.5` 两种取值——**最多差半毫米，肉眼和断言都看不出来**；
- 它只检查"这道门 `house.html` 认不认""门的数量对不对"，
  而错位的门**恰恰是合法的**，于是它一路绿灯，84 条断言全过。

**现在补上了两道锁：**

1. 随机户型（300 份）的原点偏移改成**上千毫米、正负都有**；
   每道导出的门都要和草图里画的位置对一遍，误差超过 **0.5mm**（导出时会四舍五入到整毫米）
   就判失败。另外加了一条"样本真的覆盖到了远离原点的户型"——防止以后有人把偏移改小，
   让这道锁悄悄空转。
2. 一条**定点回归锁**（测试 1b），数字是写死的，坏了能一眼看出错在哪个方向、错了多少：
   - 用例 A 就是线上那份户型：主卧被往上拖 1200（`minY = −1200`），过门石该落在 4640；
   - 用例 B：整体平移 (8100, 3700) 的横墙通铺门；
   - 用例 C：原点在 (0,0) 的老户型，结果必须和从前**逐位一样**。

**实测：把修复那一行退回去，用例 A 立刻报"错位 −1200mm"**——
而同一份代码里，"house.html 都认""housePlan 全部 ok"这两条**依然是绿的**。
这正好证明了老测试为什么瞎：它验的东西和出问题的地方根本不是同一个。

### 界面上能看到的

**没有新增任何控件**。差别就是：从草图导过去的门，现在画在它该在的那段墙上了。

### 验收

- 三个引擎测试套件全绿：`sketch-test.js` **93 通过 0 失败**（原来 84，新增 9 条）、
  `house-test.js` **179 通过 0 失败**、`test.js` **317 通过 0 失败**。
- 无头浏览器走**真实的**【应用到全屋通铺】按钮（不是模拟）：
  导出成 `at=2000`，过门石落在 y=4640，**错的量 0mm**；
  修复前同一条路径是 `at=800`、落在 3440、**差 1200mm**。
- 两个工具各截一张图肉眼比对：那道 900 宽的过门石在草图里和在全屋平面图上，
  都落在客厅↔次卧那道墙**从上往下约 55.6%** 的位置。修复前它会落在 22% 那儿。
  **两个页面全程零 JS 报错。**

### 一处已知边界（这是对的，不是没改）

**门本来就放不下的位置，仍然会被夹回墙内。** 比如门心被拖到超出这段墙的范围，
导出时会把它推回墙里最靠边的位置——这是有意的（宁可挪一点，也不能让
`house.html` 拿到一道不合法的门而整张图白屏），而且草图那边拖动的光标本来就会跟手，
正常操作碰不到。这次修的只是"明明在墙里，却被整体平移带跑"的那一类。

### 没动的

`index.html`（卫生间）和 `house.html`（全屋通铺）这两个文件**一行没改**，
它们的 `?v=` 也就没跟着动。这次只改了 `房间布局草图.html` 和它的测试
`sketch-test.js`。

---

*版本：本次更新 · 2026-09-23*
